# arXiv 大规模论文下载系统设计

> 面向初学者的系统设计说明。目标是在服务器上长期、稳定地下载约 80 万篇 arXiv 论文，并支持高并发、任务管理、断点恢复、备份、回滚和可检索的多级目录组织。本文假设服务器已经把云存储挂载到 `/mnt`，最终文件放在 `/mnt` 下。

## 1. 先理解问题：这不是一个“循环下载”脚本

最简单的程序是：读一批论文 ID，循环请求 PDF，写入文件。论文数量只有几百篇时，这种方式足够；达到 80 万篇后，问题会变成一个小型数据平台：

- 网络会抖动，远端会限流，服务器或进程会重启。
- 同一篇论文可能被重复提交，或者论文版本发生变化。
- 多台机器同时下载时，不能互相覆盖文件。
- 任务必须能暂停、继续、取消、重试，并能知道每篇论文当前状态。
- 文件写入完成不代表文件正确，必须用大小和校验和验证。
- 元数据和文件需要分别备份；恢复时还要知道某个文件属于哪个数据集版本。
- 单个目录不能放几十万文件，否则目录遍历、备份和文件系统性能都会变差。

因此，核心思想是：

> **数据库记录“应该下载什么、下载到哪一步”；队列负责分发任务；下载器只做可重试的幂等动作；对象存储或分片文件系统保存文件；清单（manifest）把元数据、文件和版本绑定起来。**

## 2. 需求边界和假设

### 2.1 主要目标

1. 导入论文列表：支持 arXiv ID、搜索结果、分类时间范围或批量清单。
2. 下载 PDF，后续可扩展为 source、v1/v2 等版本。
3. 在 80 万篇规模下保持可控吞吐，而不是无限增加并发。
4. 支持任务创建、暂停、恢复、取消、重试、查看进度。
5. 支持进程重启、机器故障后的中断恢复。
6. 支持校验、去重、备份、快照和回滚。
7. 按时间和哈希分片建立多级逻辑目录，便于人工定位和批量处理。

### 2.2 非目标

- 不在下载层负责全文解析、OCR、向量化或知识库构建。
- 不绕过 arXiv 的访问规则；并发和速率必须服从服务条款与 robots/rate limit 要求。
- 不把“HTTP 并发数越高”当作性能目标。真正目标是稳定吞吐、低失败率和可恢复性。

### 2.3 容量估算

必须先测量真实平均文件大小，再决定磁盘。一个保守的初始估算如下：

| 内容 | 假设 | 80 万篇的大致空间 |
| --- | --- | ---: |
| PDF | 平均 5 MB | 约 4 TB |
| PDF | 平均 10 MB | 约 8 TB |
| PDF + 版本冗余、临时文件、校验和、索引 | 额外 30%~100% | 约 5~16 TB |
| 两份完整备份 | 按 10 MB 平均文件 | 约 16 TB 以上 |

实际规划时还要考虑副本数、纠删码、备份保留期、磁盘故障预留空间。建议在写入达到 70% 容量前就扩容，而不是等到磁盘写满。

采用 `/mnt` 挂载云盘后，完整数据集容量主要由云端 bucket 承担；服务器本地 staging 不需要容纳 80 万篇，只要能覆盖“当前并发下载 + 等待复制到 `/mnt`”的文件即可。例如可先预留 100~500 GB，再根据峰值文件大小和挂载盘写入速度调整。同时确认云端项目的存储配额、API 请求配额和费用告警。

## 3. 总体架构：控制平面和数据平面分开

```text
                 +----------------------+
                 | arxivctl             |
                 | 命令行客户端          |
                 | 创建、暂停、查询任务  |
                 +----------+-----------+
                            | Unix Socket 或内网控制接口
                 +----------v-----------+
                 | arxivd 常驻服务       |
                 | 任务服务 / 调度器      |
                 | 分片、限速、租约、重试 |
                 +-----+------------+----+
                       |            |
                +------v---+  +-----v------+
                | 任务队列  |  | 元数据数据库 |
                | Redis/    |  | PostgreSQL  |
                | RabbitMQ  |  | 状态、清单、  |
                | Kafka     |  | 校验和、版本  |
                +------+----+  +------+------+
                       |              |
       +---------------v--------------v---------------+
       | 下载 Worker 集群                              |
       | 连接池、每主机限速、Range 续传、临时文件、校验 |
       +----------------------+------------------------+
                              |
                 +------------v-------------+
                 | 文件存储                 |
                 | /mnt 挂载云存储           |
                 | 日期 + 哈希分片目录        |
                 +------------+-------------+
                              |
                 +------------v-------------+
                 | 备份、快照、监控、告警     |
                 +--------------------------+
```

### 3.1 各组件职责

- **`arxivctl` 命令行客户端**：只负责发送命令和展示结果，不直接下载文件，也不直接修改数据库。
- **`arxivd` 常驻后台服务**：接收 CLI 命令，写入任务和数据库，持续运行调度器、租约回收和状态扫描。CLI 退出后，任务仍会继续执行。
- **任务服务**：把“下载 80 万篇”拆成批次和单篇任务，维护状态机和权限。
- **调度器**：从待处理任务中取出可执行任务，应用优先级、并发上限、主机限速和重试策略。
- **队列**：提供任务缓冲和消费者解耦。队列丢失不能导致数据丢失，因此数据库清单仍是最终事实来源。
- **元数据数据库**：保存论文、版本、任务、文件位置、校验和、错误信息和审计记录。
- **Worker**：执行一次下载尝试；必须幂等，崩溃后可以重新执行。
- **文件存储**：通过 `/mnt` 保存最终不可变文件；Worker 不依赖挂载目录本身判断任务是否成功，而以数据库和 manifest 为准。
- **备份服务**：备份数据库和文件清单，制作不可变快照，并定期做恢复演练。

### 3.1.1 运行方式：CLI + 常驻服务，而不是 WebUI

这个系统确实是一个轻量的 Client/Server 架构，但这里的“Client”只是命令行程序，不需要浏览器或 WebUI：

```text
用户执行 arxivctl
        |
        | 提交创建/暂停/恢复/查询命令
        v
arxivd 常驻后台服务（systemd/docker 保活）
        |
        +-- 调度器、任务队列、Worker
        +-- PostgreSQL、/mnt 文件存储
```

推荐部署方式：

- 在服务器上长期运行 `arxivd`，由 systemd、Docker Compose 或其他进程管理器自动启动和拉起。
- 用户在服务器上执行 `arxivctl`；如果 CLI 与服务在同一台机器，优先使用 Unix Socket，例如 `/run/arxiv/arxivd.sock`。
- 如果需要从另一台管理机提交命令，再开放只允许内网访问的控制接口，并使用认证；这仍然是内部管理 API，不代表要建设 Web 页面。
- `arxivctl task create ...` 返回 `task_id` 后即可退出；下载、重试、断点恢复由 `arxivd` 在后台继续完成。
- 机器重启后先恢复挂载和数据库，再由 `arxivd` 扫描租约过期任务并继续执行。

在 80 万篇规模下，不建议让每次 CLI 调用都直接启动一个新的下载进程。CLI 应该是短命令，`arxivd` 才是拥有调度状态和长期运行职责的进程。

### 3.2 当前部署前提：云盘已挂载到 `/mnt`

这里把 `/mnt` 称为“挂载云存储”。需要注意，`gsutil` 通常是 Google Cloud Storage 的命令行客户端，本身不是文件系统挂载程序；如果服务器能直接通过 `/mnt/...` 读写云端文件，实际提供挂载能力的可能是 `gcsfuse`、`rclone mount`、NFS 网关或其他工具。具体工具不影响整体设计，但会影响重命名、缓存和一致性语义。

部署前应确认挂载点和文件系统类型，而不是仅凭目录名称判断：

```bash
findmnt /mnt
df -Th /mnt
mount | grep '/mnt'
```

建议将路径配置成一个明确的子目录，例如：

```text
MOUNT_ROOT=/mnt
DATA_ROOT=/mnt/arxiv
```

如果 `/mnt` 本身就是专用 arXiv 云盘，也可以把 `DATA_ROOT` 设为 `/mnt`，但仍建议保留 `objects/`、`manifests/`、`quarantine/` 等子目录，避免和其他数据混放。

挂载云盘和本地磁盘有几个重要区别：

- 延迟通常更高，目录遍历和大量小文件操作可能很慢。
- `rename`、`fsync`、文件锁和随机写入不一定具有本地文件系统的原子语义。
- 挂载短暂失联时，读写可能阻塞或返回 I/O 错误。
- 云端费用通常与请求次数、出站流量和存储量有关。

因此，下载器不应把 `/mnt` 当作高速临时盘，也不应假设“写完后立刻对所有 Worker 可见”。

## 4. 存储设计：文件、元数据和清单分离

### 4.1 为什么推荐对象存储

80 万个文件并不适合全部放在一个目录或单块磁盘上。Google Cloud Storage、S3、MinIO、Ceph 等对象存储天然支持：

- 按 key 前缀组织多级目录；
- 大文件分段上传和断点续传；
- 对象版本、生命周期、跨节点副本；
- 通过 ETag/校验和验证内容；
- 将冷数据迁移到低成本存储。

对象存储里的“目录”实际上是 key 的前缀，不是真实目录，因此不会出现传统文件系统中同样的目录问题。不过通过挂载层访问时，列出超大前缀仍可能非常慢，所以至少应使用年月分区；是否再加哈希分片，要根据实际目录规模和挂载性能决定。

### 4.2 推荐对象 key 规则：直接 ID 是默认方案，哈希是可选分片

arXiv ID（包括版本号）本身已经是稳定的唯一标识。因此，当前场景建议先使用**提交日期/版本日期 + 原始 arXiv ID + 版本号**：

```text
objects/
  pdf/
    2024/
      01/
        2401.01234v2.pdf
  source/
    2024/01/2401.01234v2.tar
  manifests/
    dataset-2024-01-15/
      part-000001.jsonl.zst
```

其中：

- `2024/01` 是论文版本提交时间的年月；
- `2401.01234v2` 保留 arXiv 版本号；
- `manifests` 保存某次任务或数据集的清单，而不是重新复制文件。

直接使用 ID 的优点是人工可读、定位简单、无需计算哈希，也方便从 arXiv URL 反查本地文件。按年月分区后，每个目录通常已经远小于 80 万个文件；是否需要继续拆分，应以 `/mnt` 挂载层实测的列举耗时、写入延迟和目录文件数为准。

如果实测某个年月目录仍然过大，再增加哈希分片，例如：

```text
objects/pdf/2024/01/2f/2401.01234v2.pdf
```

这里的 `2f` 才是 SHA-256 的前 2 个十六进制字符。原文中同时使用 `2f/2f3a`（前 2 和前 4）属于更激进的两级分片，会创建更多目录；80 万篇不一定需要这么深。哈希分片的作用是均匀分散文件和降低单前缀热点，不是为了保证唯一性；完整 arXiv ID 仍然是文件名和数据库主键。

可按下面的规则选择：

| 方案 | 适用情况 | 取舍 |
| --- | --- | --- |
| 年/月 + 直接 ID | 默认方案，目录规模可控、需要人工查找 | 最易读；挂载层列举压力较大时再升级 |
| 年/月 + 1 个哈希前缀 + ID | 某些年月目录达到挂载盘性能阈值 | 分布更均匀，但路径不如直接 ID 直观 |
| 年/月 + 两级哈希 + ID | 文件量进一步扩大或挂载层对单前缀很敏感 | 目录更小，但目录数量多、人工定位更依赖数据库 |

旧式 arXiv ID 可能包含斜杠，例如 `hep-th/9901001v2`。落盘前必须做确定性的路径规范化，不能让原始斜杠意外创建目录；可以编码为 `hep-th__9901001v2.pdf`，或统一放到 `legacy/hep-th/9901001v2.pdf`，并把原始 ID 保存在数据库中。

### 4.2.1 是否使用“年/月/日/arXiv ID”

可以使用，而且对按日期导入、归档和人工检查很方便：

```text
objects/pdf/submitted/2024/01/15/2401.01234v1.pdf
objects/pdf/submitted/2024/01/15/2401.01234v2.pdf
```

但必须先明确路径中的日期含义：

| 日期字段 | 含义 | 是否适合做稳定目录 |
| --- | --- | --- |
| `submitted_at` | 某个版本首次提交时间 | 适合；对象写入后通常不移动 |
| `updated_at` | 版本修订或元数据更新时间 | 谨慎；v2、v3 可能被放到不同日期 |
| `downloaded_at` | 本服务器实际下载时间 | 不适合；重试或迁移会改变路径 |

如果目标是保存所有版本，建议每个版本使用它自己的提交日期，并把完整版本 ID（如 `2401.01234v2`）放入文件名。如果只保存最新版本，建议仍保留版本号和数据库中的旧版本记录，不要因为路径变化而覆盖旧文件。

按日分目录的主要优点：

- 可以直接处理某一天或某一周的增量任务；
- 目录内文件数量更小，挂载盘列举和人工浏览通常更轻；
- 备份、清理和数据集切片可以按日期执行；
- 不需要额外哈希也能获得自然分片。

主要代价和风险：

- 每年约 365 个日期目录，年份很多时目录层级和清单数量会增加；
- 某些高峰日期可能仍有很多文件，不能假设“按日后一定不会过大”；
- 如果使用 `updated_at`，同一论文不同版本会散落在不同日期，按论文查找必须依赖数据库；
- arXiv 的现代 ID 已经包含年月信息，年月日路径会重复一部分信息；
- 旧式 ID 仍可能含斜杠，需要先做路径规范化。

因此，对你的 80 万篇全量下载，推荐优先采用：

```text
/mnt/arxiv/objects/pdf/submitted/YYYY/MM/DD/{normalized_arxiv_id_with_version}.pdf
```

例如：

```text
/mnt/arxiv/objects/pdf/submitted/2024/01/15/2401.01234v2.pdf
```

同时在数据库中保留 `arxiv_id`、`version`、`submitted_at`、`updated_at`、`object_key`。日期目录用于组织和批处理，数据库才是论文到文件的权威映射。如果压测发现某些日期目录仍然影响 `/mnt` 性能，再在 `DD` 后增加一层哈希前缀即可。

不要把标题放入 key：标题可能包含斜杠、特殊字符，也可能发生变化。标题放在数据库和元数据 JSON 中。

### 4.3 挂载云盘下的目录方案

最终文件放在 `/mnt` 下时，建议先使用日期和直接 ID；只有压测证明需要时才加入哈希层：

```text
/mnt/arxiv/
  objects/pdf/submitted/2024/01/15/2401.01234v2.pdf
  objects/source/submitted/2024/01/15/2401.01234v2.tar
  manifests/{dataset_id}/part-000001.jsonl.zst
  quarantine/{task_id}/{paper_id}.bad
  incoming/{task_id}/{paper_id}.part
```

不要在 `/mnt` 的根目录直接放 80 万个文件，也不要用标题命名。当前推荐路径是 `objects/pdf/submitted/年/月/日/ID`；如果压测后加哈希层，则使用 `objects/pdf/submitted/年/月/日/哈希前缀/ID`。数据库中的 `object_key` 才是最终定位依据。

### 4.4 推荐的两级落盘流程

挂载盘适合保存最终结果，但不一定适合承担下载过程中的大量随机写入。推荐使用本地高速盘作为 staging：

```text
本地临时盘                         挂载云盘（最终位置）
/var/cache/arxiv/{task_id}/        /mnt/arxiv/incoming/{task_id}/
        |                                      |
        | 下载、Range 续传、校验                 | 复制/上传完成文件
        +------------ SHA-256 验证 -------------+
                                               |
                          /mnt/arxiv/objects/pdf/2024/01/.../paper.pdf
```

具体步骤：

1. Worker 首选在本地 staging 目录写 `.part` 文件，避免每个网络数据包都产生云盘写请求。
2. 下载完成后在本地计算 SHA-256，并检查 PDF 文件头、大小和 HTTP 响应。
3. 校验通过后，将文件复制到 `/mnt/arxiv/incoming/{task_id}/`，仍使用 `.part` 后缀。
4. 复制完成后读取目标文件大小；条件允许时再对目标文件做校验和抽查。
5. 只有目标文件确认完整，才将其发布到 `objects/...` 并写入数据库 artifact。
6. 清单发布成功后，才清理本地 staging 和 `incoming` 临时文件。

如果本地磁盘空间不足，可以把 staging 放在 `/mnt/arxiv/incoming`，但此时要降低并发、设置写入超时，并接受断点续传速度和云盘请求费用可能变差的事实。

不要依赖挂载盘的 `rename` 一定是原子的。最稳妥的判断依据是数据库中的 `artifact.status=complete` 和 manifest；文件名后缀只能作为辅助信号。若实测挂载工具明确保证同一文件系统内的原子重命名，可以使用“`.part` 写完后 rename 为 `.pdf`”的优化，但仍需保留 SHA-256 和数据库状态。

### 4.5 防止挂载掉线后误写系统盘

这是 `/mnt` 方案中最需要防范的故障。挂载失效后，路径 `/mnt` 可能仍存在，但它已经变成服务器本地根分区上的普通目录。程序如果继续写入，会悄悄占满系统盘。

建议采用四层保护：

1. 启动 Worker 前执行 `mountpoint -q /mnt` 或 `findmnt /mnt`，失败则拒绝启动。
2. 云盘根目录预先放置只读哨兵文件，例如 `/mnt/arxiv/.mount_sentinel`；每次开始一批任务前确认它存在且内容正确。
3. systemd 服务使用类似 `RequiresMountsFor=/mnt` 的依赖，让下载服务晚于挂载启动、早于卸载停止。
4. 监控系统盘与挂载盘的设备 ID、容量和写入延迟；设备 ID 突然变化时自动暂停调度。

健康检查不能通过“尝试创建哨兵文件”来完成，因为挂载失效时这会在系统盘上创建一个同名文件，反而掩盖故障。哨兵文件必须在云盘正常挂载时预先创建，运行时只读取。

### 4.6 去重和版本语义

- 去重主键建议为 `normalized_arxiv_id + version`。
- 文件内容用 SHA-256 标识；不同 ID 如果内容完全相同，可以共享同一个对象，但数据库要保留各自的论文引用关系。
- 默认保留每个版本；如果只需要最新版本，可把旧版本标记为 `superseded`，不要立即删除，先通过生命周期策略转冷存储。

## 5. 元数据模型：数据库是恢复的依据

推荐 PostgreSQL（或同等级关系数据库）。关键表如下：

### 5.1 `papers`

| 字段 | 说明 |
| --- | --- |
| `paper_id` | 内部 UUID 或 bigint |
| `arxiv_id` | 规范化 ID，如 `2401.01234` |
| `version` | `1`、`2`；未知时不要默认为最新 |
| `title` | 标题 |
| `submitted_at` | 该版本提交时间 |
| `categories` | 分类列表 |
| `source_url` | 原始 URL |
| `metadata_json` | 原始 Atom/API 元数据 |

唯一约束：`(arxiv_id, version)`。

### 5.2 `artifacts`

一篇论文可以有 PDF、source、tex 等多个文件。

| 字段 | 说明 |
| --- | --- |
| `artifact_id` | 文件记录 ID |
| `paper_id` | 关联论文 |
| `kind` | `pdf`、`source` |
| `object_key` | 存储 key |
| `size_bytes` | 完成后的字节数 |
| `sha256` | 完成后的校验和 |
| `status` | `missing`、`complete`、`corrupt`、`deleted` |
| `created_at` | 写入时间 |

唯一约束可设为 `(paper_id, kind)`；内容级去重则另建 `blobs(sha256, size_bytes, object_key)`。

### 5.3 `download_tasks`

| 字段 | 说明 |
| --- | --- |
| `task_id` | 单篇任务 ID |
| `dataset_id` | 所属批次/数据集 |
| `paper_id` | 论文 |
| `priority` | 优先级 |
| `state` | 状态机状态 |
| `attempts` | 已尝试次数 |
| `next_retry_at` | 下次可重试时间 |
| `lease_owner` | 当前 Worker |
| `lease_expires_at` | 租约过期时间 |
| `bytes_downloaded` | 断点进度 |
| `last_error_code` | 可分类错误码 |
| `last_error_message` | 脱敏后的错误摘要 |
| `updated_at` | 最后更新时间 |

唯一约束：`(dataset_id, paper_id, kind)`。这样重复提交同一批次不会生成重复下载。

### 5.4 `datasets` 和 `manifests`

`datasets` 表示一次逻辑数据集或任务批次，记录创建人、过滤条件、目标数量、当前状态和发布指针。`manifests` 保存每个分片的文件清单，例如：

```json
{
  "dataset_id": "ds_2024_01",
  "paper_id": "2401.01234v2",
  "artifact": "pdf",
  "object_key": "objects/pdf/submitted/2024/01/15/2401.01234v2.pdf",
  "size_bytes": 1834021,
  "sha256": "...",
  "metadata_version": 3
}
```

清单完成并通过校验后，才把数据集状态改为 `published`。读取方只读取当前发布指针，回滚时只需把指针切换到旧清单。

## 6. 下载任务状态机

```text
PENDING -> QUEUED -> RUNNING -> VERIFYING -> SUCCEEDED
              |          |           |
              |          |           +-> CORRUPT -> RETRY_WAIT
              |          +--------------> RETRY_WAIT
              +-------------------------> CANCELLED

RETRY_WAIT -> QUEUED
RUNNING --租约过期--> QUEUED
```

状态含义：

- `PENDING`：已入库，尚未投递队列。
- `QUEUED`：等待 Worker。
- `RUNNING`：Worker 已领取租约并定期心跳。
- `VERIFYING`：下载结束，正在检查 HTTP 类型、文件头、大小和 SHA-256。
- `SUCCEEDED`：文件和清单都已确认。
- `RETRY_WAIT`：可恢复错误，等待指数退避。
- `CORRUPT`：下载到了内容但校验不通过，可按策略重试或人工处理。
- `CANCELLED`：用户取消；已完成文件默认保留，未完成临时文件可清理。

### 6.1 租约和幂等

领取任务时使用数据库事务锁（例如 `SELECT ... FOR UPDATE SKIP LOCKED`）或队列的可见性超时机制。Worker 得到一个带过期时间的租约，每隔一段时间心跳；租约过期后，调度器可以把任务重新放回队列。

所有完成操作都要带幂等条件：

1. 先检查数据库中是否已有 `SUCCEEDED` 且校验和一致的 artifact。
2. 上传对象使用确定性的 object key，重复上传不会产生第二个逻辑文件。
3. 更新状态时带上 `task_id` 和当前租约版本，旧 Worker 不能覆盖新 Worker 的结果。

## 7. 高并发设计：限速比“开更多线程”更重要

### 7.1 并发分层

建议分别控制四种并发：

1. **全局并发**：整个集群同时下载的任务数，例如初始 100~500。
2. **每 Worker 并发**：单进程同时连接数，例如 10~50。
3. **每远端主机并发**：对 `export.arxiv.org`、`arxiv.org` 等主机单独限速，例如 2~10，并设置请求间隔。
4. **挂载盘发布并发**：限制同时从 staging 复制到 `/mnt` 的文件数，例如从 4~16 开始压测，避免大量并行写拖慢整个挂载。

最终值必须通过压测和失败率调优，并遵守 arXiv 的服务规则。使用令牌桶或漏桶实现速率控制，遇到 `429` 或 `503` 时自动降低速率。

### 7.2 队列和背压

- 队列只保留“可执行任务”的缓冲，数据库保存完整任务集合。
- 当存储写入速度、数据库连接池或网络带宽达到阈值时，调度器停止继续投递，形成背压。
- 将任务按日期或哈希分区，避免一个调度器扫描 80 万行。
- 大批量导入采用分块（例如每块 1 万条），每块完成后写入 manifest checkpoint。

### 7.3 连接和内存

- 使用 HTTP keep-alive 和连接池，但设置连接空闲超时。
- 流式写入，不能把整篇 PDF 读入内存。
- 为每个任务设置最大文件大小、总耗时和读取超时，防止异常响应占满 Worker。
- 对响应头和内容做验证：HTTP 200、`Content-Type` 合理、文件头为 `%PDF-`；不要仅凭扩展名判断成功。

### 7.4 错误分类

| 错误 | 处理 |
| --- | --- |
| DNS、连接重置、超时 | 指数退避重试，带随机抖动 |
| HTTP 429、503 | 读取 `Retry-After`，降低对应主机速率 |
| HTTP 404 | 标记 `NOT_FOUND`，通常不重试或少量重试 |
| HTTP 403 | 暂停该主机队列，检查访问策略 |
| 本地 staging 写满、`/mnt` 不可用 | 暂停调度和发布，告警，扩容或挂载恢复后做一致性检查 |
| PDF 校验失败 | 删除 `.part` 或重新从已知偏移续传；多次失败后人工审查 |

重试次数和退避必须有上限，例如 8~12 次；永久错误进入死信队列（DLQ），不能无限重试。

## 8. 断点续传和中断恢复

### 8.1 单文件下载流程

1. 根据 `task_id` 创建本地临时路径：`/var/cache/arxiv/{task_id}/{paper_id}.part`；若没有本地 staging，则使用 `/mnt/arxiv/incoming/{task_id}/{paper_id}.part`。
2. 如果 `.part` 已存在，读取本地长度 `N`，向服务器发送 `Range: bytes=N-`。
3. 服务器返回 `206 Partial Content` 且 `ETag`/`Last-Modified` 未变化时，追加写入。
4. 如果服务器不支持 Range，重新下载到新的临时文件，避免拼接错误。
5. 下载完成后计算 SHA-256、检查 PDF 头和大小。
6. 将校验后的文件复制到 `/mnt/arxiv/incoming/`，确认目标大小，再发布到最终 object key。不要把“复制调用返回成功”直接等同于“最终对象已经可用”。
7. 在数据库事务中写入 artifact、清理任务租约、将任务标记为 `SUCCEEDED`；manifest 生成和数据集发布单独进行。

注意：不能只依赖 HTTP Range。要保存响应的 ETag 或最后修改时间；远端内容变化时必须从 0 重新下载。

### 8.2 进程、机器、集群中断

- 进程退出：租约在超时后自动回收；本地 `.part` 文件保留，任务重新排队。
- 机器宕机：其他 Worker 接管租约；如果 staging 在本机本地盘，数据库状态仍可恢复，但该 `.part` 可能丢失，需要从 0 下载；如果 staging 在共享挂载盘上，才尝试继续 Range 续传。
- 队列丢失：调度器定期扫描数据库中 `PENDING`、`QUEUED`、租约过期的任务重新投递。
- 数据库故障：使用主从或托管高可用，恢复后依据 WAL 和最后一次 manifest checkpoint 继续。
- `/mnt` 失联：健康检查发现挂载点不可写或读回校验失败时，立即暂停新任务和“发布”步骤；不要让 Worker 无限阻塞。挂载恢复后先运行一致性扫描，再继续队列。

恢复原则是“至少一次执行 + 幂等提交”。不要追求复杂的“恰好一次下载”，因为网络世界无法可靠保证它。

## 9. 备份、快照和回滚

### 9.1 备份对象

需要分别保护三类东西：

1. **元数据数据库**：论文、任务状态、校验和、object key、审计记录。
2. **论文对象**：`/mnt/arxiv/objects` 下的 PDF/source 本身，以及尚未发布的 `incoming` 文件。
3. **清单和配置**：数据集过滤条件、调度配置、版本指针、秘钥引用。

只备份文件、不备份数据库，会失去“这个文件属于哪个任务、是否校验过”的关系；只备份数据库、不备份文件，则无法恢复数据集。

### 9.2 建议策略

- 数据库：每日全量备份 + 持续 WAL/增量备份，保留 30~90 天。
- 挂载云盘：优先使用云存储提供方的桶级版本、跨区域复制或快照；不要只把挂载目录当作唯一备份。
- 如果必须从挂载目录备份，采用分批、可恢复的同步，并记录每个文件的大小和 SHA-256；不要在下载高峰期对 `/mnt` 做全量递归扫描。
- 清单：每个已发布数据集生成不可变、带 SHA-256 的 manifest 快照。
- 配置：放入版本控制，秘钥只保存引用，不把密钥写入 Markdown 或日志。
- 定期做恢复演练，验证备份不是“看起来成功”而是真能读出文件。

### 9.3 `gsutil` 在本方案中的角色

如果 `/mnt` 背后确实是 Google Cloud Storage，应找出对应的 bucket URI（例如 `gs://source-bucket/arxiv`）。日常下载程序仍可以写 `/mnt/arxiv`，但大规模备份、列举、跨桶复制和版本管理优先走云存储原生 API 或 `gsutil`，通常比通过挂载目录递归扫描更稳定。

```bash
# 查看当前账号可访问的 bucket
gsutil ls

# 查看源 bucket 是否开启对象版本控制
gsutil versioning get gs://source-bucket

# 源 bucket 到备份 bucket 的服务端同步示意
gsutil -m rsync -r gs://source-bucket/arxiv gs://backup-bucket/arxiv
```

上面的 bucket 名只是占位符，必须替换为服务器的真实配置。备份任务第一次运行时不要添加 `rsync -d`；`-d` 会让目标端删除源端不存在的文件，这属于同步删除而不是安全备份。开启对象版本控制和合理的生命周期规则后，即使误覆盖或误删，也能在保留期内恢复旧版本。

如果不知道 `/mnt` 对应哪个 bucket，应查看挂载服务配置或进程启动参数，不要根据目录名猜测。还要确认服务账号同时拥有源 bucket 的读取权限和备份 bucket 的写入权限，并遵循最小权限原则。

### 9.4 RPO/RTO 示例

- RPO（最多丢多少数据）：数据库连续归档时可设为 15 分钟；`/mnt` 背后的对象存储按版本控制和跨桶复制策略决定。
- RTO（多久恢复服务）：单机故障目标 1 小时；大规模灾难目标 4~24 小时，取决于备用容量。

### 9.5 回滚方式

回滚应优先回滚**数据集发布指针**，而不是删除文件：

```text
current -> manifest-v3   （发现批量导入错误）
rollback -> manifest-v2   （读取服务立即恢复旧版本）
```

任务级回滚：将失败任务重新置为 `QUEUED`。数据集级回滚：把 `datasets.current_manifest_id` 指向旧清单。对象级删除只在确认超过保留期后由生命周期任务执行。

## 10. 任务管理接口（示例）

CLI 是本系统的主要用户入口。CLI 可以通过本机 Unix Socket 调用 `arxivd`；如果未来需要远程管理，再增加一个内网控制 API，不需要增加 WebUI。概念接口如下：

| 操作 | 示例 |
| --- | --- |
| 创建任务 | `arxivctl task create --input ids.txt --kind pdf --name jan-2024` |
| 查看任务 | `arxivctl task show <task-id>` |
| 查看进度 | `arxivctl task progress <task-id>` |
| 暂停 | `arxivctl task pause <task-id>` |
| 恢复 | `arxivctl task resume <task-id>` |
| 取消 | `arxivctl task cancel <task-id>` |
| 重试死信 | `arxivctl task retry-failed <task-id>` |
| 校验数据集 | `arxivctl dataset verify <dataset-id>` |
| 发布清单 | `arxivctl dataset publish <dataset-id> --manifest manifest-v3` |
| 回滚 | `arxivctl dataset rollback <dataset-id> --to manifest-v2` |

进度至少展示：总数、成功、进行中、等待重试、永久失败、已跳过、平均速度、预计剩余时间、最近错误类型和存储占用。

## 11. 监控、日志与告警

### 11.1 指标

- 下载成功率、失败率、重试率、429/503 数量。
- 每主机请求速率、平均延迟、P95/P99 延迟。
- 当前队列长度、租约过期数、死信数。
- 每分钟完成篇数、字节吞吐、预计完成时间。
- 数据库连接池、锁等待、队列积压。
- 本地 staging 使用率、`/mnt` 挂载盘容量、挂载读写延迟、写入错误、校验失败数。
- `incoming` 临时文件数量和总大小；这些文件持续增长通常表示发布流程卡住。
- 清单中 `SUCCEEDED` 但对象不存在的数量（应始终为 0）。

### 11.2 日志

每次尝试至少记录 `task_id`、论文 ID、Worker ID、目标主机、HTTP 状态、耗时、重试次数、字节数、错误分类。日志中不要记录访问令牌或完整的敏感请求头。

### 11.3 告警阈值示例

- 15 分钟成功率低于 95%。
- 某远端主机连续出现 429/403。
- 队列积压超过 30 分钟没有下降。
- 本地 staging 或 `/mnt` 使用率超过 70%/85%/95% 分级告警。
- `/mnt` 心跳探测失败、读写延迟异常或连续出现 I/O error。
- 数据库备份或对象复制连续两次失败。

## 12. 安全与合规

- API 和管理 CLI 使用身份认证与最小权限；普通用户不能删除已发布对象。
- 下载 URL、数据库凭据通过环境变量或密钥管理服务注入。
- 管理操作写审计日志：谁在何时创建、暂停、回滚或删除了什么。
- 对下载内容做基本安全检查，限制最大文件大小和解压操作；如果未来处理 source 压缩包，必须防止路径穿越和解压炸弹。
- 尊重 arXiv 访问政策、速率限制和内容许可。批量下载前应确认账号、网络出口和缓存策略符合要求。

## 13. 推荐的分阶段实现路线

### 阶段 A：单机小步迭代

阶段 A 不应该一开始就以 1~5 万篇为目标。先用几十篇做演示，确认命令行、后台服务、挂载盘路径和数据状态都能被别人直观看懂，再逐步增加规模。

#### A0：演示版（20~50 篇）

目标是演示一条完整但简单的链路，而不是证明系统已经具备生产级恢复能力。

- `arxivctl` + `arxivd` + 一个小型 Worker 池；不引入 Redis/RabbitMQ。
- 使用 PostgreSQL 保持与后续架构一致；如果只是一次性本地演示，也可以用 SQLite，但要明确这是临时替代方案。
- 从固定的 20~50 个已知 arXiv ID 开始，写入 `papers` 和 `download_tasks`。
- 下载到本地 staging 或 `/mnt/arxiv/incoming`，校验 PDF 文件头、大小和 SHA-256，再写入 `/mnt/arxiv/objects`。
- 任务状态只需要 `PENDING`、`RUNNING`、`SUCCEEDED`、`FAILED`、`CANCELLED`。
- **暂时不实现 HTTP Range 续传**。网络中断后删除 `.part` 并从头重试即可；几十篇规模下这样更容易解释和调试。
- 提供 `task create`、`task show`、`task progress`、`task cancel` 和 `dataset verify` 等最小 CLI 命令。
- 暂不做高可用、跨节点副本、复杂优先级和自动扩缩容。
- 数据库可以每天执行一次 `pg_dump`；演示任务很短时，建议在任务开始前和结束后各做一次备份，比单纯等待每日定时任务更可靠。

验收标准：

- 能向别人展示“提交任务 -> 后台下载 -> 查询进度 -> 文件出现在 `/mnt`”的完整过程。
- 20~50 篇中成功、失败和跳过状态可解释，失败任务能手动重新提交。
- 重复提交同一个任务不会生成重复的逻辑 artifact。
- 随机抽查所有成功文件，PDF 头、文件大小和 SHA-256 校验通过。
- `/mnt` 未挂载时，服务拒绝启动或暂停下载，不会写到服务器系统盘。

#### A1：小规模功能版（500~5,000 篇）

- 增加有限次数的整文件重试、指数退避、429/503 错误分类和死信列表。
- 增加暂停/恢复、批次进度、失败重试和 manifest 导出。
- 保持“失败后整文件重下”，暂时仍不实现 Range；这样可以先验证任务管理和运维流程。
- 每日数据库备份、manifest 备份和 `/mnt` 一致性扫描。
- 开始测量单日目录文件数、挂载读写延迟、本地 staging 占用和平均吞吐。

验收标准：连续运行数小时后队列能自然排空；服务重启后未完成任务可重新排队；备份恢复后能查询任务并定位文件；失败率和重试原因有统计。

#### A2：单机可恢复 MVP（1~5 万篇）

- 引入 HTTP Range 断点续传和 ETag/Last-Modified 校验。
- 使用本地 SSD staging，限制从 staging 发布到 `/mnt` 的并发。
- 增加租约、心跳、崩溃接管、定期一致性扫描和数据库索引优化。
- 在小规模压测和故障演练通过后，才进入阶段 B 的多 Worker 和更大规模运行。

验收标准：杀掉进程或模拟网络中断后可以继续；重复执行不会产生重复文件；随机抽样文件可校验通过；挂载盘短暂失联后不会误写系统盘。

### 阶段 B：多 Worker 和稳定挂载写入（10~30 万篇）

- Redis/RabbitMQ 队列。
- 使用挂载云盘作为最终文件层；如条件允许，同时保留云存储原生 API/桶作为备份或第二副本。
- 每主机令牌桶、死信队列、Prometheus/Grafana 监控。
- 数据集清单和发布指针。

验收标准：增加 Worker 后吞吐提升但远端错误率不显著增加；单 Worker 宕机不影响整体任务。

### 阶段 C：生产级高可用（80 万篇）

- PostgreSQL 高可用和 WAL 归档。
- 为 `/mnt` 背后的 bucket 配置对象版本、跨 bucket/跨区域副本或等价能力。
- 调度器高可用、租约回收、定期一致性扫描。
- 完整备份恢复演练、容量告警、审计和权限系统。
- 按日期/哈希分区的数据库索引，批量导入和清单压缩。

验收标准：模拟数据库恢复、`/mnt` 掉线、云存储不可用、网络中断、重复提交和批量回滚，均能恢复到可解释状态。

## 14. 一个完整任务的例子

用户提交 `2020-01-01` 到 `2020-12-31` 的论文列表：

1. 导入器规范化 ID，去重，并写入 `papers`。
2. 创建 `dataset-2020`，为每篇论文创建 `download_tasks`，初始为 `PENDING`。
3. 调度器分批投递 10,000 条到队列，按主机限速。
4. Worker 下载到本地 staging 的 `.part`，中断后下次用 Range 继续。
5. 完成后计算 SHA-256，复制并验证 `/mnt/arxiv/objects/...` 中的目标文件，再写入 `artifacts` 和 manifest 分片。
6. 一致性检查确认“每个成功任务都有对象、大小和校验和”。
7. 生成不可变 `manifest-v1`，将数据集发布指针切换到它。
8. 每日备份数据库和对象，保留旧 manifest。
9. 如果发现导入条件错误，只把指针切换回上一版，不删除文件。

## 15. 常见错误决策

### 只用一个 CSV 记录状态

并发更新容易覆盖，崩溃时难以恢复，也无法安全地表达租约和重试时间。应使用数据库，CSV 只作为导入/导出格式。

### 每篇论文启动一个进程

进程开销大，无法精细限速，失败恢复困难。应使用固定数量的 Worker 和异步/连接池模型。

### 下载完成后直接按标题重命名

标题不稳定且可能包含非法路径字符。应使用稳定的 arXiv ID + 版本作为主键，标题只作展示字段。

### 成功后立刻删除旧版本和临时文件

这会破坏回滚和取证。先标记、快照和校验，等超过保留期再由后台生命周期任务清理。

### 用“增加并发”解决吞吐问题

瓶颈可能在远端限流、带宽、磁盘、对象存储或数据库。应先看指标，再针对瓶颈扩容，并保持每主机限速。

## 16. 最终建议

对初次实现，建议选择下面这条路线：

```text
arxivctl（命令行客户端） -> arxivd（常驻后台服务）
                                |
PostgreSQL（状态与元数据）
    + Redis/RabbitMQ（任务缓冲）
    + /mnt 挂载云盘（论文对象与版本）
    + 本地 staging + Worker 集群（流式下载、Range、校验）
    + manifest 发布指针（快照与回滚）
    + Prometheus/Grafana（监控）
```

先用几千到几万篇做故障演练，再逐步扩大到 80 万篇。特别要模拟 `/mnt` 短暂不可用、本地 staging 磁盘写满、复制到挂载盘中途断开等情况。系统能否可靠运行，关键不在某个 HTTP 库，而在于：**数据库状态机、幂等写入、租约回收、合理限速、文件校验、不可变清单和可验证的备份恢复流程**。

## 17. 术语速查

| 术语 | 含义 |
| --- | --- |
| 幂等 | 同一个操作执行一次或多次，最终结果相同 |
| 租约（lease） | Worker 暂时拥有任务的时间窗口，过期后可被接管 |
| Range 续传 | 从已下载字节偏移继续请求文件 |
| manifest | 描述数据集包含哪些文件及其校验和的清单 |
| WAL | 数据库预写日志，用于故障恢复和增量备份 |
| RPO | 故障后最多允许丢失的数据时间范围 |
| RTO | 故障后恢复服务所需的目标时间 |
| DLQ | 死信队列，存放达到重试上限仍失败的任务 |
| 背压 | 下游变慢时限制上游继续投递，防止系统被压垮 |
