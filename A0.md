arXiv 下载系统 A0 演示版实施指南
1. 阶段目标
A0 用 20~50 篇论文验证一个完整、可演示的下载闭环：
arxivctl 提交任务
    -> arxivd 后台执行
    -> 查询下载进度
    -> 校验 PDF
    -> 写入 /mnt
    -> 查看成功和失败结果
A0 的重点是证明以下功能成立：
CLI 客户端可以创建和查询任务。
CLI 退出后，后台服务仍继续下载。
下载任务状态可以持久化。
PDF 能按日期和 arXiv ID 写入 /mnt。
重复提交不会产生重复文件。
服务重启后可以重新执行未完成任务。
/mnt 未挂载时不会误写系统盘。
成功文件具有大小和 SHA-256 校验信息。
2. A0 明确不做的功能
为了控制规模，以下功能推迟到 A1/A2：
不实现 HTTP Range 断点续传。
不实现每日自动数据库备份。
不使用 Redis、RabbitMQ 或 Kafka。
不实现分布式 Worker。
不实现高可用和主从切换。
不实现 WebUI。
不实现自动扩缩容。
不实现复杂任务优先级。
不实现跨区域对象复制。
不实现完整的数据集发布和回滚系统。
下载中断后，A0 直接删除不完整 .part 文件并从头下载。
数据库只保留可选的手工导出能力，不配置定时备份任务。
3. 固定技术方案
建议 A0 使用以下技术：
模块	选择
开发语言	Python 3.11+
CLI	Typer 或 Click
常驻服务	FastAPI + Uvicorn
CLI 与服务通信	127.0.0.1 内部 HTTP
数据库	SQLite 3（默认）或 PostgreSQL（可选）
ORM/迁移	SQLAlchemy + Alembic
HTTP 下载	httpx
后台并发	asyncio，最多 2 个下载
最终存储	/mnt/arxiv
临时文件	/var/cache/arxiv-downloader
日志	标准输出 + journald
服务管理	systemd


A0 使用 127.0.0.1 而不是 Unix Socket，是为了方便初学者调试和演示。服务不能监听 0.0.0.0，避免控制接口暴露到公网。
4. A0 架构
用户
 |
 | arxivctl task create/show/progress
 v
arxivctl 命令行客户端
 |
 | HTTP，仅监听 127.0.0.1
 v
arxivd 常驻后台服务
 |
 +-- 控制接口
 +-- 简单调度循环
 +-- 2 个下载 Worker
 +-- SQLite 3 / PostgreSQL
 |
 +--> 本地 staging
 |    /var/cache/arxiv-downloader
 |
 +--> 最终挂载盘
      /mnt/arxiv
arxivd 是唯一负责修改任务状态的进程。arxivctl 不直接连接数据库，也不直接下载论文。
5. 目录规划
5.1 程序目录
arxiv-downloader/
  pyproject.toml
  migrations/
  src/
    arxiv_downloader/
      cli/
      daemon/
      database/
      downloader/
      storage/
      metadata/
      models/
  tests/
  deploy/
    arxivd.service
  demo/
    demo_ids.txt
5.2 服务器运行目录
/etc/arxiv-downloader/
  config.toml

/var/lib/arxiv-downloader/
  exports/

/var/cache/arxiv-downloader/
  {task_id}/
    {paper_id}.part

/run/arxiv-downloader/

/mnt/arxiv/
  .mount_sentinel
  incoming/
    {task_id}/
      {paper_id}.part
  objects/
    pdf/
      submitted/
        YYYY/
          MM/
            DD/
              {normalized_arxiv_id_with_version}.pdf
  manifests/
  quarantine/
最终文件示例：
/mnt/arxiv/objects/pdf/submitted/2024/01/15/2401.01234v2.pdf
旧式 ID：
hep-th/9901001v2
规范化为：
hep-th__9901001v2.pdf
原始 ID 必须保存在数据库中。
6. 最小数据库模型
A0 只需要四张核心表。
6.1 batches
表示用户提交的一次批量下载任务。
字段	含义
batch_id	UUID
name	任务名称
state	RUNNING、COMPLETED、CANCELLED
total_count	总论文数
created_at	创建时间
completed_at	完成时间


6.2 papers
字段	含义
paper_id	内部 UUID
arxiv_id	不带版本的规范化 ID
version	版本号
title	标题
submitted_at	该版本提交时间
pdf_url	PDF 地址
metadata_json	原始元数据


唯一约束：
(arxiv_id, version)
6.3 download_tasks
字段	含义
task_id	UUID
batch_id	所属批次
paper_id	论文
state	任务状态
attempts	已尝试次数
last_error_code	错误分类
last_error_message	错误摘要
started_at	开始时间
finished_at	完成时间
updated_at	更新时间


唯一约束：
(batch_id, paper_id)
6.4 artifacts
字段	含义
artifact_id	UUID
paper_id	论文
kind	A0 固定为 pdf
object_key	/mnt/arxiv 下的相对路径
size_bytes	文件大小
sha256	SHA-256
status	COMPLETE 或 CORRUPT
created_at	创建时间


唯一约束：
(paper_id, kind)
7. A0 任务状态机
PENDING
   |
   v
RUNNING
   |
   +--------> SUCCEEDED
   |
   +--------> FAILED
   |
   +--------> CANCELLED
A0 不需要 RETRY_WAIT、VERIFYING、租约和心跳等复杂状态。
重试时：
FAILED -> PENDING
服务重启时：
RUNNING -> PENDING
因为 A0 只有一个 arxivd，启动时可以安全地把遗留的 RUNNING 任务重新置为 PENDING。
8. 最小 CLI
8.1 检查服务
arxivctl daemon status
8.2 创建任务
arxivctl task create \
  --name demo-01 \
  --input demo/demo_ids.txt
返回：
Batch created: 9a2d...
Papers accepted: 20
Duplicates skipped: 1
Invalid IDs: 1
8.3 查看任务
arxivctl task show 9a2d...
8.4 持续查看进度
arxivctl task progress 9a2d... --watch
示例输出：
Total:      20
Pending:     3
Running:     2
Succeeded:  14
Failed:      1
Progress:   75%
8.5 取消任务
arxivctl task cancel 9a2d...
8.6 重新执行失败任务
arxivctl task retry-failed 9a2d...
8.7 校验结果
arxivctl dataset verify 9a2d...
9. 内部控制接口
这些接口只供 arxivctl 使用，不是 WebUI。
方法	路径	用途
GET	/health	服务健康状态
POST	/v1/batches	创建批次
GET	/v1/batches/{id}	查询批次
POST	/v1/batches/{id}/cancel	取消批次
POST	/v1/batches/{id}/retry-failed	重试失败任务
POST	/v1/batches/{id}/verify	校验批次文件


服务只能监听：
127.0.0.1:8765
A0 可以禁用 FastAPI 自带的 /docs 和 /redoc 页面。
10. 配置文件
配置文件建议放在：
/etc/arxiv-downloader/config.toml
示例结构：
[server]
host = "127.0.0.1"
port = 8765

[database]
dsn_env = "ARXIV_DATABASE_URL"
url = "sqlite+aiosqlite:////var/lib/arxiv-downloader/arxiv.db"

[download]
concurrency = 2
request_timeout_seconds = 120
max_attempts = 2
max_file_size_mb = 200
min_request_interval_seconds = 3
user_agent = "arxiv-downloader-a0/0.1 contact@example.com"

[storage]
mount_root = "/mnt"
data_root = "/mnt/arxiv"
staging_root = "/var/cache/arxiv-downloader"
sentinel_file = "/mnt/arxiv/.mount_sentinel"
SQLite 是 A0 默认数据库，`url` 可以使用绝对路径，也可以使用相对于配置文件目录的路径。
如果设置了 `ARXIV_DATABASE_URL`，其值优先于配置文件中的 `url`。PostgreSQL 密码不要直接写入配置文件，应通过该环境变量提供：
ARXIV_DATABASE_URL=postgresql+asyncpg://arxiv:password@127.0.0.1/arxiv
SQLite 也可以通过环境变量覆盖：
ARXIV_DATABASE_URL=sqlite+aiosqlite:////var/lib/arxiv-downloader/arxiv.db
两种数据库都使用同一套 SQLAlchemy 模型和 Alembic 迁移。SQLite 必须启用外键约束，并建议启用 WAL 和 busy timeout。A0 仍然只允许一个 arxivd 进程；SQLite 不用于多进程 Worker 或高可用部署。
11. 具体实施步骤
步骤 1：准备演示论文列表
创建 demo/demo_ids.txt，选择 20~50 篇体积适中的已知论文。
测试集合建议包含：
15~40 个正常现代 ID。
1 个重复 ID。
1 个带版本号的 ID。
1 个旧式 ID。
1 个格式非法的 ID。
输入格式：
2401.01234v1
1706.03762v7
hep-th/9901001v2
非法 ID 应在任务创建阶段被拒绝，不要发送到 arXiv。
步骤 2：准备数据库
默认 SQLite：
创建数据库文件的父目录；
确保 arxivd 用户对目录具有读写权限；
通过 Alembic 创建或升级数据库。
可选 PostgreSQL：
创建数据库；
创建专用数据库用户并授予最小权限；
通过 `ARXIV_DATABASE_URL` 提供连接 URL；
通过同一套 Alembic 迁移创建或升级数据库。
完成后验证：
四张表存在；
唯一约束生效；
外键约束生效；
程序可以建立连接；
重复论文不会插入两次。
步骤 3：实现配置加载
启动时完成：
读取 TOML。
读取配置中的 SQLite URL，或读取优先级更高的数据库连接环境变量。
只接受 SQLite/aiosqlite 和 PostgreSQL/asyncpg。
检查必填配置。
输出脱敏后的最终配置。
禁止日志打印数据库密码。
配置错误时，arxivd 应直接退出。
步骤 4：实现 /mnt 挂载保护
启动前检查：
mountpoint -q /mnt
findmnt /mnt
然后只读检查：
/mnt/arxiv/.mount_sentinel
哨兵文件必须由管理员在云盘正常挂载时提前创建。
禁止启动过程中自动创建哨兵文件，因为挂载失效时可能误写系统盘。
检查失败时：
arxivd refuses to start:
MOUNT_NOT_AVAILABLE
步骤 5：实现 arXiv ID 规范化
规范化逻辑至少处理：
去除 URL 前缀。
去除 .pdf 后缀。
保留 v1、v2 等版本号。
验证现代 ID。
验证旧式 ID。
将旧式 ID 的 / 转换为安全路径形式。
生成稳定文件名。
输入：
https://arxiv.org/abs/2401.01234v2
输出：
arxiv_id = 2401.01234
version = 2
filename = 2401.01234v2.pdf
步骤 6：实现元数据获取
对每篇论文获取：
标题；
完整版本 ID；
版本提交时间；
PDF URL；
原始元数据。
需要设置：
明确的 User-Agent；
保守的请求间隔；
超时；
有限次数重试。
不能仅从 2401.01234 推导具体日期，因为 ID 只包含年月，不包含日。
步骤 7：实现任务创建
POST /v1/batches 收到 ID 列表后：
验证输入数量不超过 A0 限制，例如 50。
规范化 ID。
对输入去重。
获取元数据。
写入或复用 papers。
创建 batch。
创建 download_tasks。
返回批次 ID 和导入统计。
所有数据库写入应放在事务中。
步骤 8：实现简单调度循环
arxivd 内部运行一个循环：
查询少量 PENDING 任务。
检查批次是否已取消。
将任务更新为 RUNNING。
交给最多两个 Worker。
Worker 完成后更新状态。
没有任务时短暂等待。
A0 不需要独立消息队列。
步骤 9：实现 PDF 下载
单篇下载流程：
创建本地 staging 目录。
下载到 {paper_id}.part。
使用流式写入，不把 PDF 全部读入内存。
限制最大文件大小。
检查 HTTP 状态码。
检查文件头为 %PDF-。
计算文件大小和 SHA-256。
校验通过后进入发布流程。
A0 不发送 Range 请求。
下载失败时：
关闭响应和文件句柄。
删除不完整 .part。
记录错误分类。
如果未达到两次尝试上限，重新置为 PENDING。
达到上限后标记为 FAILED。
步骤 10：发布到 /mnt
推荐流程：
再次检查挂载点和哨兵文件。
根据 submitted_at 生成最终路径。
将本地文件复制到 /mnt/arxiv/incoming/{task_id}/。
校验 incoming 文件大小。
计算或抽查 incoming 文件 SHA-256。
移动到最终 objects/pdf/submitted/... 路径。
再次检查最终文件存在且大小正确。
写入 artifacts。
将任务更新为 SUCCEEDED。
删除本地 staging 和 incoming 文件。
数据库只有在最终文件验证成功后，才能记录 COMPLETE。
步骤 11：实现幂等
执行任务前检查：
数据库是否已有 COMPLETE artifact。
对应最终文件是否存在。
文件大小是否和数据库一致。
必要时重新计算 SHA-256。
如果全部一致：
任务直接标记为 SUCCEEDED，不重新下载
如果文件存在但数据库没有记录：
校验文件；
一致则补建 artifact；
不一致则移入 quarantine。
步骤 12：实现启动恢复
arxivd 启动时：
检查 /mnt。
检查数据库。
将遗留 RUNNING 任务改为 PENDING。
清理超过保留时间的本地 .part。
不尝试续传。
从头重新执行任务。
这可以演示“服务重启后任务继续”，但不包含单文件断点续传。
步骤 13：实现日志
每条关键日志包含：
batch_id
task_id
arxiv_id
state
attempt
worker_id
duration_ms
size_bytes
error_code
错误码至少包括：
INVALID_ID
METADATA_NOT_FOUND
HTTP_404
HTTP_429
HTTP_5XX
DOWNLOAD_TIMEOUT
FILE_TOO_LARGE
NOT_A_PDF
CHECKSUM_FAILED
MOUNT_NOT_AVAILABLE
STORAGE_WRITE_FAILED
日志中不得包含数据库密码、访问令牌或完整敏感请求头。
步骤 14：实现手工数据库导出
A0 不配置每日定时备份。
可以提供一个手工命令：
arxivctl database export \
  --output /var/lib/arxiv-downloader/exports/demo-before.sqlite3
SQLite 使用在线 backup API 生成一致性数据库副本，PostgreSQL 使用 `pg_dump` 自定义格式。
使用 PostgreSQL 时可以将输出文件命名为 `demo-before.dump`。
建议在演示前后各执行一次。
该功能只是演示保护措施，不作为生产备份方案。
步骤 15：配置 systemd
systemd 负责：
开机启动；
异常退出后重启；
等待 /mnt 挂载；
使用专用低权限用户运行；
把日志写入 journald。
服务应依赖：
network-online.target
/mnt 挂载
使用本机 PostgreSQL 时可以额外依赖 postgresql.service；使用 SQLite 或远程 PostgreSQL 时不应强制该依赖。
不要使用 root 运行下载服务。
12. 测试清单
12.1 单元测试
现代 ID 规范化。
旧式 ID 规范化。
非法 ID 拒绝。
最终路径生成。
SHA-256 计算。
状态转换。
重复 ID 去重。
12.2 集成测试
SQLite 写入、外键和回滚。
PostgreSQL 写入和回滚（启用 PostgreSQL 的部署必须执行）。
同一份 Alembic 迁移可以在 SQLite 和 PostgreSQL 上升级。
创建批次。
查询进度。
下载有效 PDF。
处理 404。
处理 HTML 冒充 PDF。
重复提交不重复下载。
最终文件写入 /mnt。
artifact 与文件一致。
12.3 故障测试
下载过程中停止 arxivd。
重启后任务重新排队。
下载中途断网。
/mnt 不可用时拒绝发布。
staging 空间不足。
数据库暂时不可用。
最终文件存在但数据库记录不存在。
数据库记录成功但文件不存在。
13. 演示流程
演示前：
确认 SQLite 数据库目录可写，或 PostgreSQL 连接正常。
确认 /mnt 已挂载。
确认哨兵文件存在。
启动 arxivd。
执行手工数据库导出。
准备 20~50 个 ID。
正式演示：
arxivctl daemon status
arxivctl task create \
  --name public-demo \
  --input demo/demo_ids.txt
arxivctl task progress <batch-id> --watch
展示最终目录：
/mnt/arxiv/objects/pdf/submitted/YYYY/MM/DD/
运行校验：
arxivctl dataset verify <batch-id>
可以停止并重启 arxivd，展示未完成任务会重新执行。
14. A0 完成标准
满足以下条件即可认为 A0 完成：
20~50 篇演示任务可以通过 CLI 创建。
CLI 退出后后台继续下载。
进度统计准确。
有效论文可以写入正确日期目录。
非法 ID、404、超时和非 PDF 响应可解释。
下载中断后可以整文件重试。
服务重启后未完成任务可以重新排队。
重复提交不会生成重复 artifact。
每个成功文件都有大小和 SHA-256。
/mnt 未挂载时不会误写系统盘。
所有功能都可以通过命令行演示。
没有 Range、Redis、WebUI 和每日自动备份等超出范围的实现。
15. 进入 A1 的条件
只有 A0 演示稳定后才进入 A1。A1 再增加：
500~5,000 篇规模测试；
暂停和恢复；
更完整的自动重试；
每日数据库备份；
manifest 导出；
/mnt 一致性扫描；
更完整的监控指标。
