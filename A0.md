# arXiv 下载系统 A0 演示版实施指南

## 1. 阶段目标

A0 用 20~50 篇论文验证一个完整、可演示的下载闭环：

```text
arxivctl 提交任务
    -> arxivd 后台执行
    -> 查询下载进度
    -> 校验 PDF
    -> 写入 /mnt
    -> 查看成功和失败结果
```

A0 的重点是证明以下功能成立：

- CLI 客户端可以创建和查询任务。
- CLI 退出后，后台服务仍继续下载。
- 下载任务状态可以持久化。
- PDF 能按日期和 arXiv ID 写入 /mnt。
- 重复提交不会产生重复文件。
- 服务重启后可以重新执行未完成任务。
- /mnt 未挂载时不会误写系统盘。
- 成功文件具有大小和 SHA-256 校验信息。

A0 把批量下载拆成两个显式步骤：`task create` 按论文首次提交日期查询 arXiv 并生成本地
ID 文件；`task submit` 读取 ID 文件并向 `arxivd` 提交下载批次。只有 `submit` 会创建数据库
任务并启动后台下载。

### 1.1 按提交日期范围批量下载（本次新增目标）

日期范围模式必须能够自动生成指定范围内的完整 ID 文件，再由用户显式提交下载。
本次固定验收范围为 `2020-01-01` 到 `2020-12-31`。

日期语义和查询规则固定如下：

- 以论文首个版本的提交时间（arXiv Atom 的 `published`，数据库字段名为
  `submitted_at`）作为筛选字段，不使用下载时间或最后更新时间。
- 输入日期按 UTC 的自然日解释；开始日期包含，结束日期包含。内部统一转换为半开区间
  `[2020-01-01T00:00:00Z, 2021-01-01T00:00:00Z)`，避免 `23:59:59` 的精度问题。
- arXiv API 查询使用 `submittedDate`，按提交时间升序排序，并使用分页参数持续读取，直到
  服务端返回的总结果数全部消费完。为避开单次查询的大结果集限制，跨月范围按自然月切片，
  每个月内部继续分页。当任一窗口返回 `total_results >= 10000` 时，按自然日中点递归二分，
  直到每个叶子窗口低于 10000。单页大小不得超过 API 允许的上限（默认使用 200，允许配置）。
- 每个 arXiv 基础 ID 默认只建立一个下载任务，下载查询结果中返回的版本（通常是当前最新
  版本）；A0 不展开同一论文的全部历史版本。完整版本信息仍须写入 `papers` 和原始元数据。
- `task create` 按基础 arXiv ID 去重，保留 API 返回的版本号，并将结果原子写入用户指定的
  汇总文本文件；查询失败时不得留下看似完整的目标文件。每个实际查询叶子窗口还会写入
  `<汇总文件名去掉后缀>.parts/arxiv_ids_<开始日期>_<结束日期>.txt`，文件名必须准确反映
  该分片覆盖的 UTC 日期范围。
- `task create` 不连接 `arxivd`，生成文件后也不会自动开始下载。用户确认文件后必须执行
  `task submit --name ... --input ...`。
- 日期范围生成的 ID 文件不受原有 50 行演示限制，daemon 控制接口也必须接受大于 50 条的
 输入列表。

提交之后，下载、进度查询、失败重试、校验和文件发布流程与普通 ID 文件批次相同。

### 1.2 大日期范围查询的限制与上线测试说明

当前 `task create` 已处理跨月和单窗口超过 10000 条结果的情况，但还没有完全解决临时限流、
服务端故障和单日极端结果集问题。该结论在 A0 阶段保留，后续继续改进，不阻塞当前上线测试。

当前已经实现的保护：

- 日期范围按自然月切片，不把整年作为一次 `search_query` 请求；
- 当窗口的 `total_results >= 10000` 时，按自然日中点递归二分并对所有叶子窗口全局去重；
- 每个月按 `start`/`max_results` 分页，默认每页 200 条，单页最大值限制为 2000；
- 默认请求间隔为 3 秒；
- 所有分页成功后才原子写入 ID 文件，查询中途失败不会覆盖已有文件；
- 对 HTTP 429、5xx、超时和非法 XML 返回可识别错误；
- 按基础 arXiv ID 去重，并保留结果中的版本号。

已知尚未覆盖的风险：

- 如果单个 UTC 自然日仍有 10000 条或更多结果，当前无法继续拆分到小时，会返回
  `RESULT_WINDOW_TOO_LARGE`；
- 429、5xx 和超时目前不会自动重试，也没有读取 `Retry-After`；
- `--request-interval 0` 可以关闭请求间隔，仅允许在模拟测试中使用；
- 大范围查询可能包含很多分页请求，`task create` 会运行较长时间，且当前不支持断点续跑。

诊断列表 API 错误时可以增加 `--debug`。该选项会输出日期窗口、分页 offset、实际请求 URL、
响应状态和耗时、分页计数，以及失败响应的部分正文；默认不输出这些调试信息。

#### 为什么以 10000 为切分阈值

2026-08-19 对 `submittedDate:[202001010000 TO 202001312359]` 的实际测试返回
`total_results=11949`。分页请求在 `start=9800&max_results=200` 时仍返回 HTTP 200，但在
`start=10000&max_results=200` 时稳定返回 HTTP 500。响应是 arXiv Atom 错误 feed，其中 summary
为 “The server encountered an internal error and was unable to complete your request”。这不是正常的
查询完成信号，也不能把前 10000 条当作完整结果写入文件。

因此实现必须在读取到 `total_results >= 10000` 后立即拆分当前日期窗口，而不是继续请求
`start=10000`。阈值使用“大于等于”是为了避免结果数恰好为 10000 时因服务端边界语义产生
遗漏。父窗口的探测页不写入结果，两个子窗口全部成功后再合并、按基础 ID 全局去重并写文件。

A0 上线测试阶段的操作约束：

1. 首次实际测试先使用较小日期范围验证 API、网络、User-Agent、ID 文件内容和分页计数；
2. 扩大到全年范围时保持默认 `--request-interval 3`，不要设置为 0；
3. 保存命令输出的论文总数、生成文件路径和耗时，并检查文件行数与去重后的论文数量一致；
4. 如果自动拆分后仍出现 `HTTP_429`、`HTTP_5XX` 或超时，记录调试输出并稍后重试；
5. 只有 ID 文件生成成功并人工确认后，才执行 `task submit`，由 arxivd 后台处理下载。

后续改进项（不作为当前上线测试的前置条件）：单日超限时继续按小时拆分、有限指数退避重试、
`Retry-After` 支持，以及基于本地 checkpoint 的断点续跑。

## 2. A0 明确不做的功能

为了控制规模，以下功能推迟到 A1/A2：

- 不实现 HTTP Range 断点续传。
- 不实现每日自动数据库备份。
- 不使用 Redis、RabbitMQ 或 Kafka。
- 不实现分布式 Worker。
- 不实现高可用和主从切换。
- 不实现 WebUI。
- 不实现自动扩缩容。
- 不实现复杂任务优先级。
- 不实现跨区域对象复制。
- 不实现完整的数据集发布和回滚系统。
- 不实现按分类、作者或全文关键词的复杂检索；A0 只新增按 `submittedDate` 日期范围发现论文。
- 不实现定时或持续增量抓取；日期 ID 文件和下载批次都由用户显式创建。
- 不实现同一论文全部历史版本的批量下载；A0 每个基础 ID 只创建一个版本任务。
- 下载中断后，A0 直接删除不完整 .part 文件并从头下载。
- 数据库只保留可选的手工导出能力，不配置定时备份任务。

## 3. 固定技术方案

建议 A0 使用以下技术：

| 模块 | 选择 |
| --- | --- |
| 开发语言 | Python 3.11+ |
| CLI | Typer 或 Click |
| 常驻服务 | FastAPI + Uvicorn |
| CLI 与服务通信 | 127.0.0.1 内部 HTTP |
| 数据库 | SQLite 3（默认）或 PostgreSQL（可选） |
| ORM/迁移 | SQLAlchemy + Alembic |
| HTTP 下载 | httpx |
| 后台并发 | asyncio，最多 2 个下载 |
| 最终存储 | /mnt/arxiv |
| 临时文件 | /var/cache/arxiv-downloader |
| 日志 | 标准输出 + journald |
| 服务管理 | systemd |


A0 使用 127.0.0.1 而不是 Unix Socket，是为了方便初学者调试和演示。服务不能监听 0.0.0.0，避免控制接口暴露到公网。

## 4. A0 架构

```text
用户
 |
 | arxivctl task submit/show/progress
 v
arxivctl 命令行客户端
 |
 | HTTP，仅监听 127.0.0.1
 v
arxivd 常驻后台服务
 |
 +-- 控制接口
 +-- 简单调度循环
 +-- 2 个下载 Worker
 +-- SQLite 3 / PostgreSQL
 |
 +--> 本地 staging
 |    /var/cache/arxiv-downloader
 |
 +--> 最终挂载盘
      /mnt/arxiv
```

arxivd 是唯一负责修改任务状态的进程。arxivctl 不直接连接数据库，也不直接下载论文。

## 5. 目录规划

### 5.1 程序目录

```text
arxiv-downloader/
  pyproject.toml
  migrations/
  src/
    arxiv_downloader/
      cli/
      daemon/
      database/
      downloader/
      storage/
      metadata/
      models/
  tests/
  deploy/
    arxivd.service
  demo/
    demo_ids.txt
```

### 5.2 服务器运行目录

```text
/etc/arxiv-downloader/
  config.toml

/var/lib/arxiv-downloader/
  exports/

/var/cache/arxiv-downloader/
  {task_id}/
    {paper_id}.part

/run/arxiv-downloader/

/mnt/arxiv/
  .mount_sentinel
  incoming/
    {task_id}/
      {paper_id}.part
  objects/
    pdf/
      submitted/
        YYYY/
          MM/
            DD/
              {normalized_arxiv_id_with_version}.pdf
  manifests/
  quarantine/
```

最终文件示例：

```text
/mnt/arxiv/objects/pdf/submitted/2024/01/15/2401.01234v2.pdf
```

旧式 ID：

```text
hep-th/9901001v2
```

规范化为：

```text
hep-th__9901001v2.pdf
```

原始 ID 必须保存在数据库中。

## 6. 最小数据库模型

A0 使用五张核心表，其中 `batch_inputs` 保存提交后等待后台解析的 ID。

### 6.1 batches

表示用户提交的一次批量下载任务。

| 字段 | 含义 |
| --- | --- |
| batch_id | UUID |
| name | 任务名称 |
| state | RUNNING、COMPLETED、CANCELLED |
| total_count | 总论文数 |
| created_at | 创建时间 |
| started_at | 第一个输入或下载任务被 worker 领取的时间 |
| completed_at | 完成时间 |
| metadata_worker_count | 实际处理该 batch 的 metadata worker 数量 |
| download_worker_count | 实际处理该 batch 的下载 worker 数量 |

### 6.2 papers

| 字段 | 含义 |
| --- | --- |
| paper_id | 内部 UUID |
| arxiv_id | 不带版本的规范化 ID |
| version | 版本号 |
| title | 标题 |
| submitted_at | 论文首个版本的提交时间；用于日期范围筛选和 submitted 目录 |
| pdf_url | PDF 地址 |
| metadata_json | 原始元数据，至少保留 published、updated 和完整版本 ID |


唯一约束：
```text
(arxiv_id, version)
```

### 6.3 download_tasks

| 字段 | 含义 |
| --- | --- |
| task_id | UUID |
| batch_id | 所属批次 |
| paper_id | 论文 |
| state | 任务状态 |
| attempts | 已尝试次数 |
| rate_limit_wait_ms | 该输入各次 metadata 请求在共享限速器处的累计等待毫秒数 |
| last_error_code | 错误分类 |
| last_error_message | 错误摘要 |
| started_at | 开始时间 |
| finished_at | 完成时间 |
| updated_at | 更新时间 |


唯一约束：
```text
(batch_id, paper_id)
```

### 6.4 artifacts

| 字段 | 含义 |
| --- | --- |
| artifact_id | UUID |
| paper_id | 论文 |
| kind | A0 固定为 pdf |
| object_key | /mnt/arxiv 下的相对路径 |
| size_bytes | 文件大小 |
| sha256 | SHA-256 |
| status | COMPLETE 或 CORRUPT |
| created_at | 创建时间 |


唯一约束：
```text
(paper_id, kind)
```

### 6.5 batch_inputs

表示已提交但尚未完成元数据解析的输入 ID。

| 字段 | 含义 |
| --- | --- |
| input_id | UUID |
| batch_id | 所属批次 |
| arxiv_id | 规范化的基础 ID |
| version | 用户指定版本；未指定时为空，由后台解析当前版本 |
| state | PENDING、RUNNING、SUCCEEDED、FAILED、CANCELLED |
| attempts | 元数据解析尝试次数 |
| rate_limit_wait_ms | 该输入各次 metadata 请求在共享限速器处的累计等待毫秒数 |
| last_error_code | 最近一次解析错误分类 |
| last_error_message | 错误摘要 |
| started_at | 本次解析开始时间 |
| finished_at | 解析完成时间 |
| updated_at | 更新时间 |

唯一约束：
```text
(batch_id, arxiv_id, version)
```

## 7. A0 任务状态机

```text
PENDING
   |
   v
RUNNING
   |
   +--------> SUCCEEDED
   |
   +--------> FAILED
   |
   +--------> CANCELLED
```

A0 不需要 RETRY_WAIT、VERIFYING、租约和心跳等复杂状态。

重试时：
```text
FAILED -> PENDING
```

服务重启时：
```text
RUNNING -> PENDING
```

因为 A0 只有一个 arxivd，启动时可以安全地把遗留的 RUNNING 任务重新置为 PENDING。

## 8. 最小 CLI

### 8.1 检查服务

```bash
arxivctl daemon status
```

### 8.2 按日期范围生成 ID 文件

```bash
arxivctl task create \
  --start-date 2020-01-01 \
  --end-date 2020-12-31 \
  --output demo/arxiv_ids_2020.txt
```

`--start-date` 和 `--end-date` 按 UTC 自然日解析，结束日期包含。`create` 直接分页查询 arXiv，
但不连接 `arxivd`；`--submitted-from` 和 `--submitted-to` 作为兼容别名保留。未提供
`--output` 时默认生成 `arxiv_ids_<开始日期>_<结束日期>.txt`。

返回：

```text
ID file created: /path/to/demo/arxiv_ids_2020.txt
Papers: <count>
Submission dates (UTC): 2020-01-01 to 2020-12-31 inclusive
```

### 8.3 向 arxivd 提交任务

```bash
arxivctl task submit \
  --name submitted-2020 \
  --input demo/arxiv_ids_2020.txt
```

返回：

```text
Batch created: 9a2d...
Papers queued: <count>
Duplicates skipped: 0
Invalid IDs: 0
```

### 8.4 查看任务

```bash
arxivctl task show 9a2d...
```

### 8.5 持续查看进度

```bash
arxivctl task progress 9a2d... --watch
```

示例输出：

```text
Metadata pending:  0
Metadata running:  1
Metadata imported: <count>
Metadata failed:   1
  Metadata failed ID: 2001.00119v2
  Metadata ID: 2403.05530v1
Total:      20
Pending:     3
Running:     2
  Downloading: 2310.06825v1
  Downloading: 2501.12948v2
Succeeded:  14
Failed:      1
Progress:   75%
Errors (historical): 1
  Error: 2001.00119v2 [METADATA_NOT_FOUND] (time: 30.5s) metadata not found
```

`task show`/`task progress` 在 metadata worker 或下载 worker 正在处理任务时分别显示
`Metadata ID` 和 `Downloading`；多个 worker 同时运行时会逐行列出多个 ID。`--watch` 模式会
反复刷新当前列表；错误会标记为历史错误，不会表示 daemon 正在重复下载同一论文。旧批次若
保存了空错误信息，CLI 会提示使用 `arxivd --debug` 检查日志；新发生的 HTTP 异常会保存异常
类型或具体信息，不再只留下空消息。元数据解析失败的论文会持久保存在 `batch_inputs`，批次完成
后仍可通过 `task show <batch-id>` 或 `task progress <batch-id>` 的 `Metadata failed ID` 和历史
错误列表查询，不依赖 daemon 日志留存。历史错误行还会显示最后一次处理尝试从 `started_at`
到 `finished_at` 的执行耗时；该时间不包含任务排队时间。旧记录缺少任一时间戳时不显示耗时。

批次完成后还会显示：

```text
Queue time: 5.3s
Run time:   3m 4s
Workers:    metadata=1, download=2
Rate-limit wait (worker sum): 2m 8s (metadata 45.0s, download 1m 23s)
```

`Rate-limit wait (worker sum)` 是所有 metadata/download worker 在共享限速器入口处的累计等待
时间，包含同一次任务重试产生的等待，并分别持久化到 `batch_inputs` 与 `download_tasks`。多个
worker 可以同时等待，因此累计 worker 时间可能彼此重叠，不能直接用 `Run time` 减去该值来
推导“纯下载墙钟时间”；这组指标用于比较 worker 数量、请求间隔和重试策略的调优效果。

### 8.6 取消任务

```bash
arxivctl task cancel 9a2d...
```

### 8.7 重新执行失败任务

```bash
arxivctl task retry-failed 9a2d...
```

### 8.8 校验结果

```bash
arxivctl dataset verify 9a2d...
```

## 9. 内部控制接口

这些接口只供 arxivctl 使用，不是 WebUI。

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | /health | 服务健康状态 |
| POST | /v1/batches | 创建批次 |
| GET | /v1/batches/{id} | 查询批次 |
| POST | /v1/batches/{id}/cancel | 取消批次 |
| POST | /v1/batches/{id}/retry-failed | 重试失败任务 |
| POST | /v1/batches/{id}/verify | 校验批次文件 |


服务只能监听：

```text
127.0.0.1:8765
```

A0 可以禁用 FastAPI 自带的 /docs 和 /redoc 页面。

## 10. 配置文件

配置文件建议放在：

```text
/etc/arxiv-downloader/config.toml
```

示例结构：

```toml
[server]
host = "127.0.0.1"
port = 8765

[database]
dsn_env = "ARXIV_DATABASE_URL"
url = "sqlite+aiosqlite:////var/lib/arxiv-downloader/arxiv.db"

[download]
concurrency = 2
request_timeout_seconds = 120
max_attempts = 2
max_file_size_mb = 200
min_request_interval_seconds = 3
user_agent = "arxiv-downloader-a0/0.1 contact@example.com"

[storage]
mount_root = "/mnt"
data_root = "/mnt/arxiv"
staging_root = "/var/cache/arxiv-downloader"
sentinel_file = "/mnt/arxiv/.mount_sentinel"
```

SQLite 是 A0 默认数据库，`url` 可以使用绝对路径，也可以使用相对于配置文件目录的路径。
如果设置了 `ARXIV_DATABASE_URL`，其值优先于配置文件中的 `url`。PostgreSQL 密码不要直接写入配置文件，应通过该环境变量提供：

```bash
ARXIV_DATABASE_URL=postgresql+asyncpg://arxiv:password@127.0.0.1/arxiv
```

SQLite 也可以通过环境变量覆盖：

```bash
ARXIV_DATABASE_URL=sqlite+aiosqlite:////var/lib/arxiv-downloader/arxiv.db
```

两种数据库都使用同一套 SQLAlchemy 模型和 Alembic 迁移。SQLite 必须启用外键约束，并建议启用 WAL 和 busy timeout。A0 仍然只允许一个 arxivd 进程；SQLite 不用于多进程 Worker 或高可用部署。

### 10.1 当前实现的启动与大任务操作参考

以下命令以仓库根目录为当前目录。开发和实际测试至少需要两个终端：第一个终端持续运行
`arxivd`，第二个终端运行 `arxivctl`。不要同时启动多个 `arxivd` 进程。

#### 环境变量

本地测试使用：

```bash
cd /path/to/arxiv_downloader
source .venv/bin/activate

export ARXIV_DOWNLOADER_CONFIG="$PWD/config/config.dev.toml"
export ARXIVD_URL="http://127.0.0.1:8765"
export ARXIV_USER_AGENT="arxiv-downloader-a0/0.1 your-email@example.com"
```

各变量的用途如下：

- `ARXIV_DOWNLOADER_CONFIG`：`arxivd`、Alembic 和数据库导出命令使用的 TOML 配置文件；
- `ARXIVD_URL`：`arxivctl` 访问控制接口的地址；未设置时默认是
  `http://127.0.0.1:8765`；
- `ARXIV_USER_AGENT`：本地执行 `task create` 查询 arXiv 列表 API 时使用，实际测试必须将
  示例地址替换为可联系的真实邮箱；
- `ARXIV_DATABASE_URL`：可选的数据库连接覆盖项，优先级高于 TOML 中的
  `[database].url`。使用 `config.dev.toml` 的默认 SQLite 时不要设置该变量。

需要覆盖数据库时，选择一种连接方式：

```bash
# SQLite 绝对路径需要四个斜杠。
export ARXIV_DATABASE_URL="sqlite+aiosqlite:////absolute/path/to/arxiv.db"

# 或使用 PostgreSQL。不要把真实密码提交到仓库。
export ARXIV_DATABASE_URL="postgresql+asyncpg://arxiv:password@127.0.0.1/arxiv"
```

`arxivd` 的下载 User-Agent、并发数、重试次数和请求间隔来自 TOML 的 `[download]`；
`ARXIV_USER_AGENT` 只作用于由 `arxivctl task create` 发起的日期列表查询。大范围实际测试前，
还应把 `config.dev.toml` 或生产配置中的 `download.user_agent` 改为包含真实联系方式的值。

#### 第一次运行的准备命令

本地开发配置使用仓库下的 `.local` 目录，不要求 `/mnt` 是真实挂载点，但仍要求哨兵文件存在：

```bash
cd /path/to/arxiv_downloader
source .venv/bin/activate
export ARXIV_DOWNLOADER_CONFIG="$PWD/config/config.dev.toml"

mkdir -p .local/storage-volume/arxiv .local/cache
touch .local/storage-volume/arxiv/.mount_sentinel
alembic upgrade head
alembic current
```

当前数据库版本应显示 `0005_batch_rate_limit_metrics (head)`。每次代码更新包含新迁移时，都应在
启动 daemon 前重新执行 `alembic upgrade head`。

#### 终端一：启动 arxivd

```bash
cd /path/to/arxiv_downloader
source .venv/bin/activate
export ARXIV_DOWNLOADER_CONFIG="$PWD/config/config.dev.toml"

arxivd
```

需要诊断 daemon、scheduler、HTTP 或数据库行为时使用：

```bash
arxivd --debug
```

`--debug` 只改变当前进程的控制台日志级别，不改变下载并发、请求间隔或数据库状态。正常 INFO
模式下，每篇新下载的 PDF 发布完成后也会输出一条简短摘要；路径相对于配置中的
`storage.data_root`：

```json
{"event":"file_saved","filename":"2403.05530v5.pdf","size":"2.5 MiB",
 "size_bytes":2621440,"object_key":"objects/pdf/submitted/2024/03/05/2403.05530v5.pdf"}
```

成功的 `HTTP/1.1 2xx` 请求行会被过滤；高频的成功型 JSON 事件（例如 `metadata_imported`、
`task_reused_artifact`）也仅在 `--debug` 时输出。非 2xx 请求和失败事件仍会保留，便于诊断。

批次完成时，`arxivd` 会输出一条包含 `queue_duration_ms`、`duration_ms`、worker 数量、
metadata/download 限速累计等待时间、`total`、
`succeeded`、`failed`、`cancelled`、`metadata_failed` 和 `metadata_failed_ids` 的
`batch_finished` JSON 事件。例如：

```json
{"batch_id":"...","queue_duration_ms":5300,"duration_ms":184320,
 "metadata_worker_count":1,"download_worker_count":2,
 "metadata_rate_limit_wait_ms":45000,"download_rate_limit_wait_ms":83000,
 "rate_limit_wait_ms":128000,"event":"batch_finished","total":20,
 "succeeded":19,"failed":0,"cancelled":0,"metadata_failed":1,
 "metadata_failed_ids":["2001.00119v2"],"state":"COMPLETED"}
```

其中 `queue_duration_ms` 从 batch 创建计算到第一个输入或下载任务被 worker 领取；
`duration_ms` 从首次领取计算到 batch 完成，因此不包含调度前的排队时间。
`succeeded`/`failed`/`cancelled` 只统计已创建的下载任务，元数据解析失败单独计入
`metadata_failed`，对应 ID 同时写入 `metadata_failed_ids`。数据库仍是长期查询失败明细的
权威来源，完成事件中的 ID 列表只是即时摘要。

服务应监听 `http://127.0.0.1:8765`。该终端需要保持运行；按 `Ctrl+C` 会停止 daemon，
但数据库中的任务状态不会丢失，重新启动后未完成任务会按恢复规则继续处理。

#### 终端二：检查服务并生成 ID 文件

```bash
cd /path/to/arxiv_downloader
source .venv/bin/activate
export ARXIV_DOWNLOADER_CONFIG="$PWD/config/config.dev.toml"
export ARXIVD_URL="http://127.0.0.1:8765"
export ARXIV_USER_AGENT="arxiv-downloader-a0/0.1 your-email@example.com"

arxivctl daemon status
```

先使用较小日期范围做实际 API 冒烟测试：

```bash
arxivctl task create \
  --start-date 2020-01-01 \
  --end-date 2020-01-02 \
  --output .local/arxiv_ids_2020-01-01_2020-01-02.txt

wc -l .local/arxiv_ids_2020-01-01_2020-01-02.txt
```

确认小范围查询正常后，再生成全年 ID 文件。保持默认的 3 秒请求间隔，不要为真实 arXiv
查询设置 `--request-interval 0`：

```bash
arxivctl task create \
  --start-date 2020-01-01 \
  --end-date 2020-12-31 \
  --request-interval 3 \
  --output .local/arxiv_ids_2020.txt

wc -l .local/arxiv_ids_2020.txt
```

命令会保留可直接交给 `task submit` 的汇总文件，并为每个实际查询窗口生成按真实日期命名的
分片。例如上述输出同时包含：

```text
.local/arxiv_ids_2020.txt
.local/arxiv_ids_2020.parts/arxiv_ids_2020-01-01_2020-01-16.txt
.local/arxiv_ids_2020.parts/arxiv_ids_2020-01-17_2020-01-31.txt
```

实际边界由每个窗口的 `total_results` 决定，以上日期仅用于展示命名格式。提交时仍使用汇总
文件 `.local/arxiv_ids_2020.txt`，不需要逐个提交分片文件。

遇到 `HTTP_429`、`HTTP_5XX`、超时或长时间没有输出时，使用同一命令增加调试选项：

```bash
arxivctl task create \
  --start-date 2020-01-01 \
  --end-date 2020-01-31 \
  --request-interval 3 \
  --output .local/arxiv_ids_2020-01.txt \
  --debug
```

失败响应正文最多输出 2048 个字符；调试日志不输出请求头、环境变量或数据库配置。

`task create` 在本机直接访问 arXiv API，不要求 `arxivd` 已启动；它会在完整查询成功后才写入
目标文件。当前该命令本身是前台长任务，尚不支持断点续跑。

#### 提交并监控大任务

确认 ID 文件后提交给 daemon：

```bash
arxivctl task submit \
  --name submitted-2020 \
  --input .local/arxiv_ids_2020.txt
```

`task submit` 只持久化输入并快速返回。记下输出中的 UUID，例如：

```text
Batch created: 9a2d0000-0000-0000-0000-000000000000
```

为后续命令设置一个方便使用的 shell 变量：

```bash
export BATCH_ID="9a2d0000-0000-0000-0000-000000000000"
```

随时查看一次当前状态：

```bash
arxivctl task show "$BATCH_ID"
```

持续轮询元数据解析和 PDF 下载进度，大任务建议使用 5 秒查询间隔：

```bash
arxivctl task progress "$BATCH_ID" --watch --interval 5
```

`task show` 返回单次状态快照；`task progress --watch` 会显示 metadata pending/running/imported/
failed，以及下载 pending/running/succeeded/failed 等计数，直到批次进入终态。按 `Ctrl+C` 只会
退出本地监控命令，不会取消批次，也不会停止 `arxivd`。稍后可在任意终端使用同一个
`BATCH_ID` 再次执行 `task show` 或 `task progress --watch`。

任务完成后校验已发布文件：

```bash
arxivctl task show "$BATCH_ID"
arxivctl dataset verify "$BATCH_ID"
```

需要主动取消或重试已经失败的下载任务时使用：

```bash
arxivctl task cancel "$BATCH_ID"
arxivctl task retry-failed "$BATCH_ID"
```

#### Linux/systemd 启动参考

生产配置使用 `/etc/arxiv-downloader/config.toml`、真实的 `/mnt` 挂载和专用 `arxivd` 用户。
管理员必须先确认挂载，再创建哨兵文件；daemon 不会自动创建它：

```bash
mountpoint -q /mnt
findmnt /mnt
sudo install -d -o arxivd -g arxivd /mnt/arxiv /var/lib/arxiv-downloader /var/cache/arxiv-downloader
sudo -u arxivd touch /mnt/arxiv/.mount_sentinel
```

可选数据库覆盖写入 `/etc/arxiv-downloader/arxivd.env`，不要提交到仓库：

```text
ARXIV_DATABASE_URL=postgresql+asyncpg://arxiv:password@127.0.0.1/arxiv
```

安装 `deploy/arxivd.service` 和配置文件、执行迁移后启动服务：

```bash
sudo install -d -m 0755 /etc/arxiv-downloader
sudo install -m 0644 config/config.example.toml /etc/arxiv-downloader/config.toml
sudo install -m 0644 deploy/arxivd.service /etc/systemd/system/arxivd.service

# 编辑 download.user_agent，并核对数据库、挂载和容量阈值。
sudoedit /etc/arxiv-downloader/config.toml

# 默认 SQLite：在项目安装目录中执行迁移。
sudo -u arxivd /bin/sh -c \
  'cd /opt/arxiv-downloader && ARXIV_DOWNLOADER_CONFIG=/etc/arxiv-downloader/config.toml .venv/bin/alembic upgrade head'

sudo systemctl daemon-reload
sudo systemctl enable --now arxivd
sudo systemctl status arxivd
sudo journalctl -u arxivd -f
```

如果使用 `/etc/arxiv-downloader/arxivd.env` 中的 `ARXIV_DATABASE_URL` 覆盖数据库，迁移也要
加载同一文件：

```bash
sudo -u arxivd /bin/sh -c \
  'set -a; . /etc/arxiv-downloader/arxivd.env; set +a; cd /opt/arxiv-downloader; export ARXIV_DOWNLOADER_CONFIG=/etc/arxiv-downloader/config.toml; exec .venv/bin/alembic upgrade head'
```

Linux 上的 `arxivctl` 命令与前述示例相同，默认连接本机
`http://127.0.0.1:8765`；批量任务仍通过 `task show` 和 `task progress --watch` 监控。

## 11. 具体实施步骤

### 步骤 1：准备演示输入

ID 文件演示仍创建 demo/demo_ids.txt，选择 20~50 篇体积适中的已知论文。
测试集合建议包含：

- 15~40 个正常现代 ID。
- 1 个重复 ID。
- 1 个带版本号的 ID。
- 1 个旧式 ID。
- 1 个格式非法的 ID。

输入格式：

```text
2401.01234v1
1706.03762v7
hep-th/9901001v2
非法 ID 应在任务创建阶段被拒绝，不要发送到 arXiv。
```

日期范围演示固定使用：

```text
start_date = 2020-01-01
end_date = 2020-12-31
```

该模式直接调用 arXiv 列表查询并分页，先生成 ID 文件，再由 `task submit` 提交，不要求
用户手工准备 ID 文件。

### 步骤 2：准备数据库

默认 SQLite：

- 创建数据库文件的父目录；
- 确保 arxivd 用户对目录具有读写权限；
- 通过 Alembic 创建或升级数据库。

可选 PostgreSQL：

- 创建数据库；
- 创建专用数据库用户并授予最小权限；
- 通过 `ARXIV_DATABASE_URL` 提供连接 URL；
- 通过同一套 Alembic 迁移创建或升级数据库。

完成后验证：

- 五张表存在（包括 `batch_inputs`）；
- 唯一约束生效；
- 外键约束生效；
- 程序可以建立连接；
- 重复论文不会插入两次。

### 步骤 3：实现配置加载

启动时完成：

- 读取 TOML。
- 读取配置中的 SQLite URL，或读取优先级更高的数据库连接环境变量。
- 只接受 SQLite/aiosqlite 和 PostgreSQL/asyncpg。
- 检查必填配置。
- 输出脱敏后的最终配置。
- 禁止日志打印数据库密码。
- 配置错误时，arxivd 应直接退出。

### 步骤 4：实现 /mnt 挂载保护

启动前检查：
```bash
mountpoint -q /mnt
findmnt /mnt
```

然后只读检查：
```text
/mnt/arxiv/.mount_sentinel
```

哨兵文件必须由管理员在云盘正常挂载时提前创建。
禁止启动过程中自动创建哨兵文件，因为挂载失效时可能误写系统盘。
检查失败时：
```text
arxivd refuses to start:
MOUNT_NOT_AVAILABLE
```

### 步骤 5：实现 arXiv ID 规范化

规范化逻辑至少处理：

- 去除 URL 前缀。
- 去除 .pdf 后缀。
- 保留 v1、v2 等版本号。
- 验证现代 ID。
- 验证旧式 ID。
- 将旧式 ID 的 / 转换为安全路径形式。
- 生成稳定文件名。

输入：
```text
https://arxiv.org/abs/2401.01234v2
```

输出：
```text
arxiv_id = 2401.01234
version = 2
filename = 2401.01234v2.pdf
```

### 步骤 6：实现元数据获取

对每篇论文获取：

- 标题；
- 完整版本 ID；
- 首个版本提交时间和当前版本更新时间；
- PDF URL；
- 原始元数据。

需要设置：

- 明确的 User-Agent；
- 保守的请求间隔；
- 超时；
- 有限次数重试。
不能仅从 2401.01234 推导具体日期，因为 ID 只包含年月，不包含日。

日期范围模式使用同一元数据 API 的 `search_query` 列表接口，不应为列表中的每个条目再发起
一次单 ID 查询。查询至少包含：

```text
search_query=submittedDate:[202001010000 TO 202001312359]
sortBy=submittedDate
sortOrder=ascending
start=<游标>
max_results=<页大小>
```

`task create` 按页解析和规范化 ID，在内存中按基础 ID 去重；只有所有分页都成功后才原子写入
目标文件。每个窗口读取第一页的 `totalResults`；达到 10000 时先按日期中点递归二分，不得发送
`start=10000` 请求。解析失败或 HTTP 429/5xx 时返回错误，不得用不完整结果覆盖目标文件。

### 步骤 7：实现 ID 文件生成和任务提交

`arxivctl task create`：

- 校验 `--start-date`、`--end-date` 和 `from <= to`；
- 查询 `submittedDate` 并按提交时间升序分页；
- 对 `total_results >= 10000` 的窗口按 UTC 日期中点递归二分；
- 按基础 arXiv ID 去重，保留完整版本 ID；
- 原子写入 `--output` 指定的汇总 ID 文件，并为实际叶子窗口生成准确日期命名的分片文件；
- 不连接 arxivd，也不创建数据库记录。

`arxivctl task submit` 读取 ID 文件并调用：
POST /v1/batches 收到 ID 列表后：
- 规范化 ID。
- 对输入去重。
- 每批最多 100 个 ID 获取元数据；批量请求失败时逐篇回退并记录具体错误。
- 写入或复用 papers。
- 创建 batch。
- 创建 download_tasks。
- 返回批次 ID 和导入统计。
- 所有数据库写入应放在事务中。
控制接口和 CLI 不再限制 50 行，允许提交日期范围生成的大型 ID 文件。`POST /v1/batches`
只持久化批次和待解析 ID 后立即返回；元数据解析、`papers`/`download_tasks` 创建以及 PDF
下载全部由 arxivd 后台执行。用户使用返回的 `batch_id` 调用 `task show` 或 `task progress`。

### 步骤 8：实现简单调度循环

arxivd 内部运行一个循环：

- 查询少量 PENDING 任务。
- 检查批次是否已取消。
- 将任务更新为 RUNNING。
- 交给最多两个 Worker。
- Worker 完成后更新状态。
- 没有任务时短暂等待。

A0 不需要独立消息队列。

### 步骤 9：实现 PDF 下载

单篇下载流程：

- 创建本地 staging 目录。
- 下载到 {paper_id}.part。
- 使用流式写入，不把 PDF 全部读入内存。
- 限制最大文件大小。
- 检查 HTTP 状态码。
- 检查文件头为 %PDF-。
- 计算文件大小和 SHA-256。
- 校验通过后进入发布流程。
A0 不发送 Range 请求。
下载失败时：

- 关闭响应和文件句柄。
- 删除不完整 .part。
- 记录错误分类。
- 如果未达到两次尝试上限，重新置为 PENDING。
- 达到上限后标记为 FAILED。

### 步骤 10：发布到 /mnt

推荐流程：

- 再次检查挂载点和哨兵文件。
- 根据 submitted_at 生成最终路径。
- 将本地文件复制到 /mnt/arxiv/incoming/{task_id}/。
- 校验 incoming 文件大小。
- 计算或抽查 incoming 文件 SHA-256。
- 移动到最终 objects/pdf/submitted/... 路径。
- 再次检查最终文件存在且大小正确。
- 写入 artifacts。
- 将任务更新为 SUCCEEDED。
- 删除本地 staging 和 incoming 文件。
数据库只有在最终文件验证成功后，才能记录 COMPLETE。

### 步骤 11：实现幂等

执行任务前检查：

- 数据库是否已有 COMPLETE artifact。
- 对应最终文件是否存在。
- 文件大小是否和数据库一致。
- 必要时重新计算 SHA-256。

如果全部一致：
- 任务直接标记为 SUCCEEDED，不重新下载

如果文件存在但数据库没有记录：

- 校验文件；
- 一致则补建 artifact；
- 不一致则移入 quarantine。

### 步骤 12：实现启动恢复

arxivd 启动时：

- 检查 /mnt。
- 检查数据库。
- 将遗留 RUNNING 任务改为 PENDING。
- 清理超过保留时间的本地 .part。
- 不尝试续传。
- 从头重新执行任务。
这可以演示“服务重启后任务继续”，但不包含单文件断点续传。

### 步骤 13：实现日志

每条关键日志包含：

- `batch_id`
- `task_id`
- `arxiv_id`
- `state`
- `attempt`
- `worker_id`
- `duration_ms`
- `size_bytes`
- `error_code`

错误码至少包括：

- `INVALID_ID`
- `METADATA_NOT_FOUND`
- `HTTP_404`
- `HTTP_429`
- `HTTP_5XX`
- `DOWNLOAD_TIMEOUT`
- `FILE_TOO_LARGE`
- `NOT_A_PDF`
- `CHECKSUM_FAILED`
- `MOUNT_NOT_AVAILABLE`
- `STORAGE_WRITE_FAILED`

日志中不得包含数据库密码、访问令牌或完整敏感请求头。

### 步骤 14：实现手工数据库导出

A0 不配置每日定时备份。
可以提供一个手工命令：

```bash
arxivctl database export \
  --output /var/lib/arxiv-downloader/exports/demo-before.sqlite3
```

SQLite 使用在线 backup API 生成一致性数据库副本，PostgreSQL 使用 `pg_dump` 自定义格式。
使用 PostgreSQL 时可以将输出文件命名为 `demo-before.dump`。
建议在演示前后各执行一次。
该功能只是演示保护措施，不作为生产备份方案。

### 步骤 15：配置 systemd

systemd 负责：

- 开机启动；
- 异常退出后重启；
- 等待 /mnt 挂载；
- 使用专用低权限用户运行；
- 把日志写入 journald。

服务应依赖：

- `network-online.target`
- `/mnt` 挂载

使用本机 PostgreSQL 时可以额外依赖 postgresql.service；使用 SQLite 或远程 PostgreSQL 时不应强制该依赖。
不要使用 root 运行下载服务。

## 12. 测试清单

### 12.1 单元测试

- 现代 ID 规范化。
- 旧式 ID 规范化。
- 非法 ID 拒绝。
- 最终路径生成。
- SHA-256 计算。
- 状态转换。
- 重复 ID 去重。
- 日期范围边界（包含 2020-01-01，包含 2020-12-31，不包含 2021-01-01）。
- 日期范围参数校验和 UTC 转换。
- 日期范围分页和跨页去重。
- 结果数达到 10000 时递归拆分，且不会请求 `start=10000`。
- 拆分后的文件名与各自覆盖的 UTC 日期范围一致，汇总文件包含全部分片并全局去重。
- 单日结果数达到 10000 时返回 `RESULT_WINDOW_TOO_LARGE`。
- 日期查询失败时不覆盖已有目标文件。

### 12.2 集成测试

- SQLite 写入、外键和回滚。
- PostgreSQL 写入和回滚（启用 PostgreSQL 的部署必须执行）。
- 同一份 Alembic 迁移可以在 SQLite 和 PostgreSQL 上升级。
- 创建批次。
- 按日期生成 ID 文件后再通过 `task submit` 创建批次。
- 大于 50 行的 ID 文件可以通过控制接口提交。
- 查询进度。
- 下载有效 PDF。
- 处理 404。
- 处理 HTML 冒充 PDF。
- 重复提交不重复下载。
- 最终文件写入 /mnt。
- artifact 与文件一致。

### 12.3 故障测试

- 下载过程中停止 arxivd。
- 重启后任务重新排队。
- 下载中途断网。
- /mnt 不可用时拒绝发布。
- staging 空间不足。
- 数据库暂时不可用。
- 列表 API 分页中断后不写入不完整目标文件。
- 列表 API 返回 429、5xx、非法 XML 时 `task create` 可解释地失败。
- 最终文件存在但数据库记录不存在。
- 数据库记录成功但文件不存在。

## 13. 演示流程

演示前：

- 确认 SQLite 数据库目录可写，或 PostgreSQL 连接正常。
- 确认 /mnt 已挂载。
- 确认哨兵文件存在。
- 启动 arxivd。
- 执行手工数据库导出。
- 准备 20~50 个 ID。

正式演示：

```bash
arxivctl daemon status
arxivctl task submit --name public-demo --input demo/demo_ids.txt
arxivctl task progress <batch-id> --watch
```

展示最终目录：

```text
/mnt/arxiv/objects/pdf/submitted/YYYY/MM/DD/
```

运行校验：

```bash
arxivctl dataset verify <batch-id>
```

可以停止并重启 arxivd，展示未完成任务会重新执行。

日期范围批量下载演示：

```bash
arxivctl task create \
  --start-date 2020-01-01 \
  --end-date 2020-12-31 \
  --output demo/arxiv_ids_2020.txt
arxivctl task submit \
  --name submitted-2020 \
  --input demo/arxiv_ids_2020.txt
arxivctl task progress <batch-id> --watch
arxivctl dataset verify <batch-id>
```

演示中应先展示生成文件的路径和论文数量，再展示 `submit` 返回的批次 ID 与下载进度。

## 14. A0 完成标准

满足以下条件即可认为 A0 完成：

- 20~50 篇演示任务可以通过 `task submit` 提交。
- 可以通过 `task create` 生成 `2020-01-01` 至 `2020-12-31` 的完整论文 ID 文件。
- 日期范围查询按 UTC 自然日和分页执行，跨页去重后数量可审计，查询失败不留下不完整目标文件。
- 查询窗口达到 10000 条时能自动按日期二分，分片文件名准确表达覆盖范围，汇总文件可直接提交。
- 生成的 ID 文件可以通过 `task submit` 提交给 arxivd，且不受 50 行限制。
- 2020 范围内的每个去重论文任务都能进入下载流程，批次最终可通过校验命令核对成功、失败和
  缺失数量，不得静默漏掉分页结果。
- CLI 退出后后台继续下载。
- 进度统计准确。
- 有效论文可以写入正确日期目录。
- 非法 ID、404、超时和非 PDF 响应可解释。
- 下载中断后可以整文件重试。
- 服务重启后未完成任务可以重新排队。
- 重复提交不会生成重复 artifact。
- 每个成功文件都有大小和 SHA-256。
- /mnt 未挂载时不会误写系统盘。
- 所有功能都可以通过命令行演示。
- 没有 Range、Redis、WebUI 和每日自动备份等超出范围的实现。

## 15. 进入 A1 的条件

只有 A0 演示稳定后才进入 A1。A1 再增加：

- 500~5,000 篇规模测试；
- 暂停和恢复；
- 更完整的自动重试；
- 每日数据库备份；
- manifest 导出；
- /mnt 一致性扫描；
- 更完整的监控指标。
